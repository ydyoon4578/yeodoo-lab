__pycache__/

# DB 적재 로컬 로그(커밋 금지)
.dbload.log
.dbload.launchd.log

# 로컬 미리보기 설정(배포와 무관)
.claude/

# 잠금 페이지 평문 원본(커밋 절대 금지 — 커밋하면 암호화가 무의미해진다)
# validate_site.py가 평문 의존 검사에 사용. 일일잡의 dirty-tree 가드에도 걸리면 안 된다.
_build/
