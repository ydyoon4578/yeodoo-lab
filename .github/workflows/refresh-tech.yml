name: Refresh technical strategy lab

# 기술적 지표 기반 타이밍·횡단면 규칙을 한 번에 다시 돌린다(build/tech_backtest.py).
#
# 주기 — 주 1회. 입력이 일봉(data/stocks.json 계열)이라 하루 단위로는 판정이 사실상 안 바뀌고,
#   23개 규칙 × 518종목 × 753일을 매일 돌릴 이유가 없다. 대신 결과가 그대로면 커밋도 안 생긴다.
#
# 다중검정 — 게시 건수가 아니라 **돌린 전체 건수**로 본페로니 임계를 다시 계산한다.
#   규칙을 추가하면 임계가 자동으로 높아진다(스크립트 안에서 처리).

# ── 성과 기준일 = **전월말**(2026-08-14 사용자 지시) ──────────────────────────
# "성과는 전월말까지로 하고 월 1회 자동 업데이트 하는 식으로 구성해."
# 백테스트가 격자를 전월말에서 끊는다(tech_backtest.asof_cut). 그래서 **한 달 안에서는
# 몇 번을 돌려도 산출이 한 바이트도 안 바뀌고 커밋도 안 생긴다.** 실질 갱신은 달이 넘어간
# 뒤 첫 실행 한 번뿐이다.
#   → 아래 crontab 에 매월 1일 슬롯을 둔다(달이 넘어간 직후 바로 반영).
#   ⚠ 주간 슬롯을 **지우지 않는다.** 산출이 같으면 커밋이 안 나므로 비용이 사실상 없고,
#     1일 실행이 실패했을 때 한 달을 기다리지 않게 해 주는 무료 재시도다.

on:
  schedule:
    - cron: '50 22 1 * *'    # 매월 1일 — 성과 기준일(전월말)이 넘어간 직후 반영한다
    - cron: '50 22 * * 5'    # 매주 토 07:50 KST — 자산 패널 뒤(지수 곡선이 그 주 패널을 쓴다)
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: refresh-tech
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      # 🚨 2026-08-05 — 기준선. 일을 시작하기 **전** 저장소가 이미 몇 건 깨져 있었는지
      #   잰다. 이게 없으면 아래 Validate 가 절대 기준이 되어, 이 잡과 상관없는
      #   결함 때문에 방금 받은 자료를 통째로 버린다(실제로 네 잡이 그랬다).
      - name: Validate baseline
        run: python build/validate_gate.py baseline
      # 🚨 투자의견 리비전 3종(x-revdrift*)의 입력이다. 캐시는 gitignore 라 러너에 없고,
      #   이 단계를 빠뜨리면 세 규칙이 **후보 0 으로 조용히 무보유**가 되어 지난 결과를 덮는다.
      #   (tech_backtest 안에 판정 보류 가드를 하나 더 뒀지만, 그건 최후 방어이지 정상 경로가
      #    아니다 — 정상 경로는 자료를 받는 것이다.)
      #   실패해도 잡을 세우지 않는다: 나머지 69종은 이 자료와 무관하고, 세 규칙은 위 가드가
      #   '판정 불가'로 낸다. 조용히 틀린 숫자보다 시끄럽게 빈 것이 낫다.
      #   ⚠ 이 단계만 외부 패키지가 필요하다 — tech_backtest 본편은 표준 라이브러리로 돈다.
      #     그래서 설치를 이 잡 맨 앞이 아니라 여기 둔다(나머지 단계의 실패 원인을 늘리지 않는다).
      - name: Install deps (rating fetch only)
        # 🚨 2026-08-19 — --prefer-binary. lxml 6.1.2 가 wheel 없이 올라와
        #   pip 이 소스 빌드로 넘어갔고 러너에 libxml2-dev 가 없어 죽었다
        #   (yfinance 의 전이 의존이라 우리가 부르지도 않는 패키지다).
        #   ⚠ 판을 손으로 박지 않는다 — 박은 수는 낡는다. wheel 이 있는 최신 판을
        #     고르게 두면 벤더가 wheel 을 올리는 대로 저절로 따라간다.
        run: pip install --prefer-binary --quiet pandas numpy yfinance
        continue-on-error: true
      - name: Fetch analyst rating history
        run: python build/fetch_ratings.py
        continue-on-error: true
      - name: Run technical backtests
        run: python build/tech_backtest.py
      - name: Rebuild verdict ledger
        run: python build/verdicts_gen.py
      # 전 전략 통합 목록 — 원본(tech·asset·archive·deploy)이 바뀌면 같이 다시 굽는다.
      # 빠뜨리면 전략 랩만 옛 목록을 보여주고 아무도 알려주지 않는다.
      - name: Rebuild unified strategy index
        run: python build/strategy_index.py
      # 랩 상세 차트는 목록과 분리된 지연 로딩 파일이다 — 목록만 다시 굽고 이걸 빠뜨리면
      # 새 전략이 화면에서 '차트 없는 전략'으로 보인다.
      - name: Rebuild lab charts
        run: python build/strategy_charts.py
      # 복제 리포트는 랩 세 파일에서 값을 **옮긴** 것이다. 랩만 갱신되고 이걸 빠뜨리면
      # 같은 사이트의 두 화면이 다른 날짜·다른 t 를 말한다 — '채점기가 두 벌' 이다.
      # validate_site 가 기준일을 대조해 막지만, 막히기 전에 여기서 같이 굽는 것이 맞다.
      - name: Rebuild strategy replication report
        run: python build/strategy_report.py
      - name: Rebuild as-of index
        run: python build/asof_index.py
      # 🚨 2026-08-16 점검에서 찾았다 — 이 셋은 **어느 워크플로에도 없었다.** 도구는 있는데
      #   사람이 기억해야 돌아가는 상태였고, 이 저장소에서 그 고리는 이미 두 번 끊어졌다
      #   (갱신 피드 여드레 공백 · log_update 머리말의 2026-07-23~25).
      #   여기 두는 이유: 셋 다 **방금 이 잡이 다시 구운 엔진**의 산출물을 검사한다.
      #   합쳐 15초라 잡 시간에 영향이 없다.
      - name: Verify pre-registered implementations (batch12)
        run: python build/check_prereg_20260808.py
      # ⚠ --registered 다. 검산③(동점)은 PREREG-2026-08-10-REVDRIFT-RESULT.md §6 에
      #   «완화하지 않는다» 로 적어 실패로 남긴 자리라 그것만으로는 안 막고,
      #   **등록에 없는 새 실패**가 하나라도 생기면 막는다. 문턱을 고친 것이 아니다.
      - name: Verify pre-registered implementations (revdrift)
        run: python build/check_revdrift.py --registered
      # 리포트다 — 막지 않는다(이 스크립트에는 실패 경로가 없다). 자릿수 분포를 로그에
      # 남겨 두면 산출물과 화면의 반올림이 갈리기 시작할 때 눈에 띈다.
      - name: Precision report (does not block)
        run: python build/audit_precision.py
      - name: Validate
        run: python build/validate_gate.py check
      - name: Commit if changed
        run: |
          bash build/ci_push.sh "chore(data): 기술적 전략 백테스트 자동 갱신" \
            data/tech_strategies.json data/verdicts.json data/strategy_index.json data/strategy_charts.json data/strategy_report.json data/asof.json
      # 🚨 2026-08-16 점검에서 이 잡에 신선도 게이트가 없던 것을 찾았다. 관문(validate_gate)은
      #   «자료가 깨졌나» 를 보지 «잡이 돌았는데 안 움직였나» 를 안 본다. 크론이 조용히
      #   드롭되면 잡은 초록인데 산출물만 낡는다 — check_freshness.py 가 그것 때문에 있다.
      #   매주 토 + 매월 1일. 🚨 as_of 가 아니라 generated 를 본다 — 이 파일의 as_of 는 성과 기준일(전월말)이라 잡이 정상으로 돌아도 안 움직인다.
      - name: Verify freshness
        run: python build/check_freshness.py data/tech_strategies.json "기술적 전략 백테스트" 8 --key generated
