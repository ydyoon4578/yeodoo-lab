# 결과 — 배당 증액 · 이익 변동성 · 2026-08-12

등록: `build/PREREG-2026-08-12-POLICY.md` (커밋 `7546cea1`, 돌리기 **전에** 확정)
규칙 86 → **88**, 임계 3.44 → **3.45**. PIT 족 58 → **60**.

## 1. 결과 — 둘 다 기각

| 규칙 | CAGR | 소급 t | incr5.t | PIT t | 판정 |
|---|---|---|---|---|---|
| `x-divgrow` 배당 증액률 | 21.45 | 3.06 ❌ | 1.33 ❌ | 2.26 | 구별 불가 |
| `x-earnvol` 이익 변동성 최하위 | 12.19 | −0.18 ❌ | −1.53 ❌ | −0.44 | **열위** |

## 2. 등록 §5 에 적은 예측과 대조

> "① `x-earnvol` 은 **기각될 것이다**. 이웃에 `x-sue` 또는 저변동성 계열이 잡히고
> incr5.t 가 2.0 아래일 것으로 본다."

**맞았다. 그리고 예측보다 나빴다** — 기각이 아니라 **열위**(대조군보다 샤프가 낮다)다.
이웃 5개에 `특이변동성 최하위 10` 이 들어왔다(`x-sue` 는 안 들어왔다). incr5.t = −1.53.

🚨 **'분자·분모 쪼개기는 새 정보가 아니다'가 두 번 확인됐다.**

| 배치 | 규칙 | 무엇을 쪼갰나 | 단독 t | incr5.t |
|---|---|---|---|---|
| 3차 | `x-mommvol` | `x-mom12` ÷ 변동성 | **4.12** | **−0.90** |
| 4차 | `x-earnvol` | `x-sue` 의 분모만 | −0.18 | **−1.53** |

기존 규칙의 구성요소를 떼어 내 순위로 쓰는 것은 이 표본에서 정보를 만들지 않는다.
⚠ 이것을 일반 법칙으로 적지 않는다 — 두 사례이고 둘 다 이 유니버스·이 창의 결과다.

> "② `x-divgrow` 의 이웃은 `x-dy`·`x-payout` 일 것으로 본다."

**절반 맞았다.** 실측 이웃: `장부가 저평가(30%)` · **`총주주환원 상위 50`** · `FCF수익률` ·
`Piotroski` · `장기 반전`. `x-payout` 계열은 들어왔지만 `x-dy` 는 안 들어왔고, 나머지 넷이
**가치·퀄리티**다. 즉 배당을 늘리는 회사는 이 표본에서 **고배당주가 아니라 싼 회사**와
같이 움직인다. 등록 §2① 이 물은 것("수준이 아니라 변화를 보는 것이 정보인가")의 답은
**아니다** — incr5.t 1.33 으로 가치·퀄리티를 통제하면 남는 것이 없다.

## 3. 🚨 그런데 딸린 소득이 있다 — `x-divgrow` 는 **생존편향이 거의 0 이다**

PIT 초과 13.90 vs 소급 초과 13.93. **편향 +0.03%p.**

60종을 전부 재서 줄 세우면:

| |편향| 가장 작은 | | |편향| 가장 큰 | |
|---|---|---|---|---|
| **배당 증액률** | **+0.03** | | 소형주 | +49.70 |
| 복권형 회피 MAX(5) | +0.09 | | 200일선 이격도 | +43.82 |
| FCF 수익률 | +0.13 | | 12-1 모멘텀 | +43.60 |
| 고배당수익률 | +0.23 | | 고저가 스프레드 | +30.99 |

**중앙 |편향| 4.32%p · 평균 8.40%p.** `x-divgrow` 는 60종 중 가장 작다.

읽는 법: 배당을 늘리는 회사를 고르는 규칙은 **지수에서 빠진 종목을 다르게 대하지 않는다.**
반대로 모멘텀·소형주·이격도는 초과수익의 **거의 전부**가 '오늘 살아남은 종목을 과거로
소급했다'에서 나온다. 이것은 이 랩이 여러 번 말해 온 것의 정량적 판이다.
⚠ **편향이 작다는 것이 좋은 규칙이라는 뜻은 아니다.** `x-divgrow` 는 ⓐ(3.06<3.45)와
  ⓑ(1.33<2.0)에서 떨어졌다. 편향이 없어도 정보가 없으면 게시하지 않는다. 둘은 다른 질문이다.

⚠ 이 규칙의 PIT 은 등록 §3 에 적은 대로 **반쪽이다**(편출 145종 중 dps 71종 = 49%).
  편향이 0 에 가까운 것도 그 한계 안에서 읽어야 한다 — 배당을 주던 편출 종목이 자료가 없어
  빠졌다면 두 레그가 비슷해 보이는 쪽으로 기운다. **이 수치를 근거로 삼지 않는다.**

## 4. 누적 — 11종 등록 · 11종 기각 · 게시 0종

88종: 구별 불가 79 · 판정 불가 1 · 열위 8.

오늘 ⓐ(단독 t)를 넘은 규칙은 여전히 둘뿐이다(`x-amihud` 6.84 · `x-mommvol` 4.12).
둘 다 게시되지 않았다 — 하나는 PIT 을 잴 수 없어서, 하나는 이웃을 통제하니 사라져서.
