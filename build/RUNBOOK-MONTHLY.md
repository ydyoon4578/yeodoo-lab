# 월간 절차 — 10년 창을 한 달 굴린다

> 사용자 지시 **2026-09-20**: *"월초에 한번씩은 최근 한달 백테스팅까지 진행해서
> 10년 백테스팅 업데이트해야함. 모든전략"*
> 그리고 그 앞선 지시 **2026-08-14**: *"성과는 전월말까지로 하고 월 1회 자동 업데이트
> 하는 식으로 구성해."*

---

## 0. 먼저 — **대부분은 이미 자동이다**

새로 만들 것이 아니라 **빠진 곳만** 채운다.

| 무엇 | 어떻게 | 언제 | 창 |
|---|---|---|---|
| 종목 규칙 **125종** | `refresh-tech.yml` → `tech_backtest.py` | **매월 1일** + 매주 토 | `MAX_YEARS=10` · `asof_cut` 이 **전월말**에서 끊는다 |
| 자산배분 **64종** | `refresh-assets.yml` → `asset_backtest.py` | 매일(평일) | `MAX_YEARS=10` · `cap_start()` |
| 지표 | `refresh-metrics.yml` | **매월 1일** + 매주 토 | — |
| 통합 목록·차트·리포트 | `refresh-tech.yml` 안 | 위와 같이 | — |
| 시점정확(PIT) 레그 | `pit_backtest.py` (로컬) | 손으로 | 🚨 **9.3년이 천장**이다 — 아래 §0-1 |
| 검증 | `validate.yml` | 매일 | — |

**전월말에서 격자를 끊으므로 한 달 안에서는 몇 번 돌려도 산출이 한 바이트도 안 바뀐다.**
실질 갱신은 달이 넘어간 뒤 첫 실행 한 번이고, **10년 창은 그때 저절로 한 달 굴러간다.**

### 🚨 0-1. PIT 레그는 **10년을 못 채운다** — 9.3년이 천장이다

`MAX_YEARS = 10` 은 **상한이지 보장이 아니다.** PIT 계열에서는 자료가 먼저 막는다:

| | |
|---|---|
| 멤버십 시작 | `data/index_history.json` — **2014-06** |
| 패널의 가드 | `style_pit_panel.prepare` §124 — 창 시작보다 **3년 앞**까지 멤버십을 요구 |
| 그래서 허용 시작 | **2017-06** |
| **실제 천장** | **2335거래일 = 9.3년** |

10년으로 돌리면 패널이 소리 내어 죽는다 —
`멤버십이 빈 월말 8개(2013-10-31 …) — 마스크가 풀려 PIT 이 성립하지 않는다`.

> **그러니 PIT 계열 결과를 «10년» 이라고 적지 않는다.**
> 이 천장은 멤버십이 2014-06 보다 앞으로 가기 전에는 안 올라간다.
> 출처: `PREREG-2026-09-21-STYLE8ROT-RESULT.md` §2.

⚠ 그리고 **`style_pit.json` 의 창은 둘이다.** 2026-09-21 부터 산출물의 `windows` 가 필드마다
어느 창인지 적는다 — `start`·`n_days`·`n_months` 는 **1년 레그 창**이고(`n_rebal 11` 도 같은 말),
5년은 `windows.panel`(편출 주입 명단·`universe` 집계)이다. 그 전 판은 맨 위 `n_month_ends 62` 가
**패널의 월말 수**라 레그 길이로 오독됐다(STYLE8ROT 등록이 그 탓에 두 번 고쳐졌다).

---

> 🚨 **이 클론에서 `tech_backtest.py` 를 손으로 돌리지 않는다.**
> `data/_ratings_cache.json` 이 없어 투자의견 3종이 조용히 무보유가 되고 게시물을 덮는다.
> **월간 갱신은 러너(GitHub Actions)가 한다.**

---

## 1. 월초에 할 것 — 한 줄

```
python build/monthly_refresh.py --run
```

그 스크립트가 도는 것은 **위 잡들이 안 건드리는 다섯**뿐이다:

| | 무엇 | 왜 |
|---|---|---|
| ① | `fund_card.py` | 우량성장 30 은 규칙 엔진 밖이라 어느 잡도 안 굽는다 |
| ② | `regime_table.py` | 새 달의 FF 팩터 칸 |
| ③ | `single_table.py` | 단일 신호 정본 표 |
| ④ | **`forward_track.py --append`** | 🚨 사전등록 전방 추적 |
| ⑤ | `quarterly_report.py` | 깃발만 읽는다(쓰지 않는다) |

---

## 2. 🚨 ④가 이 절차의 핵심이다 — 얼린 것과 굴리는 것

«모든 전략을 월 1회 갱신» 과 «사전등록 결과는 얼린다» 는 **충돌이 아니다.**

| | 무엇 | 규칙 |
|---|---|---|
| **판정(등록)** | 결과문서가 인용한 수 | **절대 안 바꾼다.** 말없이 바뀌면 그 문서가 거짓이 된다 |
| **운용 추적** | 등록 뒤 실제로 어떻게 갔나 | **매월 쌓는다.** 안 쌓으면 «표본 밖에서 확인한다» 가 영영 말로만 남는다 |

`forward_track.py` 가 세 칸을 나눠 적는다:

- **얼린 값** — 등록 당시의 수. 안 바뀐다.
- **10년 굴림** — 오늘 자료로 같은 창 길이를 다시 잰 것. **참고이지 판정이 아니다.**
- **표본 밖** — 등록 뒤 새로 생긴 달만. **이것만이 진짜 확인이다.**

> **지금은 표본 밖이 0개월이다.** 등록이 2026-09-20 이라 당연하고,
> **«아직 증거 없음» 을 그대로 적는 것이 이 표의 첫 일이다.**
> **표본 밖이 12개월을 넘기 전에는 어떤 등록도 «표본 밖에서 확인됐다» 고 말하지 않는다.**

---

## 3. 돌린 뒤 손으로 보는 것 셋

1. **분기 보고의 «설계를 의심할 자리인가» 깃발** — 켜졌으면 `build/qg_2026.py` 로
   팩터 귀속을 본다(설계 탓인가 국면 탓인가).
   **설계를 진짜 의심할 조건은 미리 정해 뒀다 — 6팩터 잔차가 12개월 이상 월 −0.3%p 밑.**
2. **`build/validate_site.py` 통과** 여부.
3. **`data/_forward_track.json` 에 새 달이 붙었는가.**

---

## 4. 분기에 한 번 더

3·6·9·12월 초에는 위에 더해 —

- `quarterly_report.py --md <경로>` 로 분기 보고 한 장을 남긴다.
- 펀드 리밸런스 달이면 새 30종 명단과 **비중 상위 10을 눈으로 본다**
  (`AUDIT-2026-09-20-SHARES` §7-2 — 자릿수 사고는 거기서 드러난다).

---

## 5. 하지 않는 것

1. **사전등록 결과문서의 수를 고치지 않는다.** 창을 굴려 더 좋아 보여도 그대로 둔다.
   창을 바꿔 판정을 다시 읽으면 그것이 사후 조정이다(`build/restate_10y.py` 의 전제).
2. **`tech_backtest.py` 를 이 클론에서 손으로 돌리지 않는다**(§0 의 🚨).
3. **`refresh_facts.py` 를 다시 돌리지 않는다** — `data/fx` 518개가 게시 산출물이다.
4. **얼린 사전등록 산출물을 자동 잡에 붙이지 않는다**
   (`audit_unbuilt.py` KNOWN 에 여섯이 그렇게 적혀 있다).
