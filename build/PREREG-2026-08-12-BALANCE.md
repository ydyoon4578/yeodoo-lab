# 사전등록 — 재무상태표 두 축 · 2026-08-12, 돌리기 전에 확정

규칙 **88종** → 90종, 임계 3.45 → **3.46**. PIT 족 60 → 62.

## 0. 오염 상태 · 오늘 프로브가 죽인 축

두 규칙의 수익률을 한 번도 계산하지 않았다. 신호 함수는 넣었지만 규칙 등록 전에 백테스트를
돌리지 않았다.

⚠ 오늘 1~4차 결과를 봤다(11종 등록 · 11종 기각). 영향은 4차와 같다 — **이웃 지도의 빈 칸을
고른다.** 4차 지도에서 고립 14종의 공통점이 '가격에서 안 나오는 축'이었고, 그래서 5차는
공시·재무 쪽을 팠다.

### 🚨 오늘 프로브가 죽인 축 — 공시(SEC filings)

`data/fil/` 517종 · 15,403건에 제출일(`d`)·보고기간말(`rd`)·8-K 항목코드(`it`)가 다 있다.
제출 지연(중앙 35일 · 90분위 50일)과 8-K 항목(2.02 실적 3,053건 · 5.02 임원변동 1,635건)으로
규칙을 만들 수 있어 보였다. **그런데 창이 2022-11-29 ~ 2026-08-07, 3.7년뿐이다.**
`filings.json` 의 `window_days: 90` — 롤링 수집이다. 백테스트 창 16.6년을 못 덮는다.
→ E11 내부자(6개월) · E22 실적발표(6주)와 같은 벽이다. 지우지 않고 여기 적는다.

## 1. 왜 이것인가 — **수집돼 있는데 아무도 안 쓰는 태그**

88종 전문 검색: `유동비율` 0건 · `유동자산` 0건 · `이익잉여금` 0건.
`ca`·`cl`·`re` 는 `data/fx/` 에 다 있다(랩 84%·84%·97%).

⚠ 이 태그 다섯(`ca`·`cl`·`debt`·`re`·`dep`)은 **2026-08-04 에 "수집만 되고 배선이 없다"로
  한 번 잡혔던 바로 그 묶음**이다(tech_backtest.py:1650 주석). 그때 `load_fund` 에 배선했지만
  **규칙은 여전히 안 만들어졌다.** 배선과 사용은 또 다른 일이었다.

| 있는 것 | 축 | 빈 칸 |
|---|---|---|
| `x-lowde` 총부채 수준 · `x-cash` 현금 비중 | 자본구조 | **단기 지급능력**(유동비율) |
| `x-roe`·`x-npm`·`x-gpa` | 지금 얼마나 버나 | **얼마나 오래 벌어 왔나**(누적 유보) |

## 2. 규칙 — 둘. 변형 없음.

> Ou·Penman (1989), "Financial statement analysis and the prediction of stock returns",
> *Journal of Accounting and Economics* 11(4), 295–329.
> Altman (1968), "Financial Ratios, Discriminant Analysis and the Prediction of Corporate
> Bankruptcy", *Journal of Finance* 23(4), 589–609.

### ① `x-currat` — 유동비율 상위 10

    신호 = 유동자산 ÷ 유동부채       (둘 다 잔고 항목이므로 asof_fund · ttm 아님)
    상위 10종목 동일가중 · 월말 리밸런스 · 유동부채 ≤ 0 이면 후보 제외

🚨 은행·보험은 유동/비유동을 구분해 보고하지 않는다. 커버 84%이고 빠지는 쪽이 무작위가 아니다
   → 이 규칙은 "유동비율이 높은 종목"이 아니라 **"유동/비유동을 구분해 보고하는 업종 안에서"**
   그런 종목이다. DATA-FACTS #1 과 같은 성질이고, 결손이 아니라 규칙의 일부다.

### ② `x-reta` — 이익잉여금 비율 상위 10

    신호 = 이익잉여금 ÷ 총자산      (Altman Z-score 의 X2 그대로)
    상위 10종목 동일가중 · 월말 리밸런스

⚠ 음수가 정상적으로 나온다(누적 결손). **자르지 않는다** — 그것도 정보다.

## 3. 밀도 · PIT

| 규칙 | 랩 커버 | **편출 커버** | PIT |
|---|---|---|---|
| `x-currat` | 84% | **84%** | ✅ 가능 |
| `x-reta` | 97% | **93%** | ✅ 가능 |

두 유니버스의 커버가 거의 같다 — `x-divgrow`(랩 73% vs 편출 49%)와 달리 **PIT 비교가 성립한다.**

## 4. 게시 기준 · 미리 정하는 것

    ⓐ |t| > **3.46**(90종)   ⓑ incr5.t ≥ 2.0   ⓒ PIT 레그도 통과

- 하나라도 못 넘으면 기각으로 싣는다. 문턱·창·상위 N·방향을 바꾼 변형을 돌리지 않는다.
- `pit_backtest.FUND_SIDS` 에 **등록과 함께 미리** 넣는다.
- 섹터중립판·십분위판을 만들지 않는다.

## 5. 미리 적는 예측

- `x-reta` 의 이웃은 **가치·퀄리티**(Piotroski · 장부가저평가 · 총주주환원)일 것으로 본다 —
  오래 벌어 유보한 회사는 장부가가 두껍기 때문이다. 그러면 incr5 에서 죽는다.
- `x-currat` 의 이웃은 **`x-cash`·`x-lowde`** 일 것으로 본다.
- ⚠ 예측이 맞든 틀리든 판정 기준은 안 바꾼다.
