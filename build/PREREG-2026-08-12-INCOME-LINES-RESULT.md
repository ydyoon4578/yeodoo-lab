# 결과 — 손익계산서 위쪽 줄의 서프라이즈 3종 · 2026-08-12

등록: `build/PREREG-2026-08-12-INCOME-LINES.md` (커밋 `d9369722`, 돌리기 **전에** 확정)
돌린 것: `python build/tech_backtest.py` 1회. 변형 없음. 재실행 없음.

## 1. 결과 — **셋 다 기각.** 두 관문 모두 못 넘었다

규칙 78 → **81**, 본페로니 임계 3.41 → **3.42**.

| 규칙 | CAGR | 샤프 | 초과 | **단독 t** | **incr5.t** | 후보 중앙 | 판정 |
|---|---|---|---|---|---|---|---|
| `x-cdisc` 비용규율 | 22.38 | 0.776 | +8.64 | **1.57** | **−0.27** | 28 | 기각 |
| `x-sur` 매출 서프라이즈 | 17.29 | 0.823 | +4.06 | **1.43** | **−1.83** | 124 | 기각 |
| `x-sugp` 매출총이익 서프라이즈 | 22.44 | 0.901 | +6.33 | **1.11** | **1.06** | 19 | 기각 |
| *(대조)* `x-sue` 이익 서프라이즈 | 20.72 | 1.029 | +7.49 | 2.82 | 2.23 | 395 | 기각 |
| *(대조)* `x-gpa` 총이익 자산대비 | — | — | — | — | — | — | 기각 |

게시 기준 ⓐ 단독 |t| > 3.42 · ⓑ incr5.t ≥ 2.0 · ⓒ PIT 레그. **ⓐ에서 이미 셋 다 탈락**했다.

⚠ 초과수익만 보면 셋 다 벤치마크를 이겼다(+4.1 ~ +8.6%p). **그것이 판정이 아니다** —
16.6년 표본에서 81개 규칙을 돌리면 그중 여럿이 우연히 그렇게 보인다. t 가 그것을 재는 자다.

## 2. 등록이 물은 것에 대한 답

> "`x-sue` 는 손익계산서 맨 아랫줄 하나만 본다. 그 위쪽 줄이 **따로** 정보를 갖는가?"

**`x-sur`(매출)에 대해서는 답이 나왔다 — 아니다.** 이 셋 중 유일하게 제대로 검정된
규칙이고(14.6년 · 후보 중앙 124), `incr5.t = −1.83` 이다. 이웃 5개를 통제하면 매출
서프라이즈는 **이웃들보다 못하다.** 부호가 음수라는 것이 요점이다 — '조금 부족했다'가
아니라 '이 축은 이미 다른 규칙들이 더 잘 잡고 있다'는 뜻이다.

🚨 **`x-sugp`·`x-cdisc` 에 대해서는 답이 안 나왔다 — 못 쟀다.** 아래 §3 이 이유다.
   이 둘을 '기각'으로 싣되, **'검정해서 졌다'가 아니라 '제대로 검정되지 않았다'** 로 읽어야 한다.
   ⚠ 그렇다고 다시 돌리지 않는다. 등록 §3 이 "문턱·창·상위 N 을 바꾼 변형을 새로 돌리지
     않는다"고 못 박았고, 결과를 보고 그 약속을 무르면 사전등록이 무의미해진다.

## 3. 🚨 프로브가 틀렸다 — **한 시점만 재고 이력을 안 쟀다**

등록 §2 에 커버리지를 이렇게 적었다: `rev∩cogs` **294종(56.6%)**. 그 숫자는 맞다.
**2026-06-30 시점에 대해서만** 맞다.

돌리고 나서 후보 풀 이력을 재 보니:

| 시점 | `x-sur` | `x-sugp` | `x-cdisc` |
|---|---|---|---|
| 2012-06 | 67 | **8** | **11** |
| 2016-06 | 109 | **13** | **19** |
| 2018-06 | 119 | **15** | 77 |
| 2020-06 | 254 | 35 | 254 |
| 2026-06 | 472 | 276 | 301 |

`x-sugp` 의 후보가 **8종 → 276종, 34배**로 불어난다. 2012 년의 '상위 10'은 8종 중에서
고른 것이라 **선별이 아니라 전량 보유**다. `XSEC_MIN_POOL`(30)에 걸려 실제로는:

    x-sugp   창 6.1년(2020-06~) · 얇은 달 125개 · 후보 중앙 19
    x-cdisc  창 8.1년(2018-06~) · 얇은 달 101개 · 후보 중앙 28
    x-sur    창 14.6년(2011-12~) · 얇은 달 23개 · 후보 중앙 124   ← 이것만 제대로 검정됐다
    x-sue    창 14.6년 · 얇은 달 23개 · 후보 중앙 395             (대조군)

즉 `x-sugp` 와 `x-sue` 를 같은 표에 놓고 비교한 것 자체가 **다른 기간·다른 두께의 시험**이었다.

### 왜 놓쳤나 — 프로브의 설계 결함

`build/probe_batch12.py` 는 `fx/*.json` 을 열어 "rev 와 cogs 가 **둘 다 있는 종목 수**"를 셌다.
그건 **파일 단위** 질문이고, 백테스트가 묻는 것은 **월말 단위** 질문이다("2014-03 시점에
그때까지 공개된 자료로 채점 가능한 종목이 몇인가"). 태그가 있기만 하면 세었으니, 그 태그가
2019 년부터 시작한 종목도 2012 년 후보로 셈된 것이다.

⚠ 이 함정은 `build/DATA-FACTS.md` **#3(후보 풀 램프)** 에 이미 적혀 있었다. 읽고도 프로브를
그렇게 짰다 — 문서가 있는 것과 그 문서대로 재는 것은 다른 일이다.

    → 교훈: 횡단면 규칙의 프로브는 **반드시 월별(또는 연별) 후보 수를 시계열로** 내야 한다.
      한 시점 커버리지는 그 규칙이 언제부터 돌아가는지에 대해 아무것도 말하지 않는다.
    → `probe_batch12.py` 에 그 결함을 주석으로 박아 둔다. 지우지 않는다 — 지우면 다음 사람이
      같은 프로브를 다시 짠다.

## 4. 딸린 관찰 — 규칙 81종 중 **통과 0종**

이번 셋을 더한 뒤 전체 판정: **구별 불가 75 · 열위 6 · 통과 0.**
`x-sue` 자신도 t 2.82 로 임계 3.42 를 못 넘는다. 즉 이 랩에서 **실적 서프라이즈 축은
통째로 다중검정 문턱 아래**에 있다. 이번 셋의 기각은 그 계열의 일부다.

임계가 3.41 → 3.42 로 오르면서 판정이 뒤집힌 기존 규칙은 **없다.** 경계에 가장 가까운
`퍼센트 영업발생액 회피`(t 3.42)는 incr5.t 0.27 이라 어차피 ⓑ에서 걸린다.

## 5. 약속대로 — 지우지 않는다

세 규칙 다 `build/tech_backtest.py` 에 그대로 남긴다. 기각된 규칙을 목록에서 빼면
다중검정 족 수가 줄어 남은 규칙이 쉽게 통과한다. 다음 배치의 임계는 81종 기준이다.
