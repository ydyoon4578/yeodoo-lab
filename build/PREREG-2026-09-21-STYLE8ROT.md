# 사전등록 — 스타일 로테이션 (STYLE8ROT) · 2026-09-21

풀카드: 없음
규칙: `x-style-fmom` · `x-style-fmom8`
선행: `PREREG-2026-09-18-STYLE8W(-RESULT).md` — 가중 방식 셋 **전부 기각**

---

## 0. 이 등록은 랩이 **이미 예고한 자리**다

STYLE8W 등록서 §0 이 이렇게 적어 뒀다 —

> *"🚨 **이 등록은 로테이션이 아니다.** 상태변수가 없고 조건부 분기가 없다.
> 로테이션(조건부)은 이 등록이 끝난 뒤 **`STYLE8ROT`** 의 일이고, **그때 대조군은
> 반드시 여기 결과여야 한다** — FACROT 이 「조건부가 무조건보다 나빴다」를 남겼기 때문이다."*

**그 지시를 그대로 따른다. 대조군은 `ew`(8종 동일가중)다.**

## 1. 🚨 먼저 — 이 등록이 걸어 들어가는 벽 셋

**① 8종의 유효 자산 수가 2.45 / 8 이다.**
STYLE8W §1-0 의 실측이다. **8자산이 아니라 사실상 2.45자산**이고,
그래서 가중 방식 셋이 전부 무력했다. **로테이션도 같은 벽을 만난다** —
돌릴 것이 2.45개뿐이면 무엇을 고르든 비슷해진다.

**② `ew` 가 벤치에 진다.** 표본 밖 49개월 샤프 — 벤치 **0.92** · ew 0.89 · erc 0.89 ·
iv 0.87 · mv 0.65. **지는 것들 사이에서 돌리는 것**이 이 등록의 출발점이다.

**③ 랩은 국면 타이밍을 열 번 물어 아홉 번 기각했다.**
FACROT(조건부 < 무조건) · CGATE(t 0.86) · a2-factor-rot(**−1.81%p · Δ샤프 −0.119**) ·
오늘까지 국면 조건부 규칙 10건 중 9건 기각.

> **그래서 내 사전 기대는 «안 된다» 이고, 예측 P1 에 그렇게 적는다.**
> 그래도 묻는 이유는 §2 다 — **아직 안 물어본 신호가 하나 있다.**

## 2. 문헌 — 무엇을 신호로 쓸 것인가

로테이션 문헌은 **두 진영**으로 갈린다.

### ⓐ 밸류에이션 타이밍 — **다툼이 있다**

| | |
|---|---|
| **Hodges·Hogan·Peterson·Ang (2017)** JPM 44(1) · BlackRock | 횡단면(팩터 간 상대 밸류·분산도) + 시계열(경기·자기 밸류)을 **합치면** 낫다 |
| **Asness (2016)** *The Siren Song of Factor Timing* JPM | 팩터 타이밍은 역사적으로 **매우 약하고**, 장기 효과 검정은 과장됐거나 적용 불가 |
| **Asness·Chandra·Ilmanen·Israel (2017)** *Contrarian Factor Timing is Deceptively Difficult* JPM 43(5) | 가치·모멘텀·방어 셋에서 **밸류에이션 기반 타이밍의 견고한 증거를 못 찾았다.** 회전율이 높으면 현재 보유의 밸류에이션이 예측력을 잃는다 |
| **Bender·Sun 외 (2018)** *The Promises and Pitfalls of Factor Timing* JPM 44(4) | 예측변수마다 **유효 시계(horizon)가 다르다** — 시계를 안 맞추면 안 된다 |

🚨 **랩의 `a2-factor-rot` 이 이 진영을 이미 시도했고 졌다.** 다만 그 규칙은
**논문의 핵심인 밸류에이션이 아예 없었다** — 200일선과 VIX뿐이었다.
그래서 «밸류에이션 타이밍을 제대로 검정했다» 고 말할 수 없다.

### ⓑ 팩터 모멘텀 — **증거가 더 단단하다**

> **Gupta·Kelly (2019)** *Factor Momentum Everywhere*, JPM 45(3)
> 전 세계 **65개 팩터**에서 «팩터 자신의 최근 성과» 로 그 팩터를 타이밍할 수 있고,
> 전체를 묶은 시계열 팩터 모멘텀 포트폴리오의 **연 샤프 0.84**.
> 종목 모멘텀·산업 모멘텀으로는 **설명되지 않는다.**

**⚠ 반론도 있다** — Alpha Architect 의 *"Is factor momentum really everywhere?"* 처럼
견고성을 되묻는 후속 작업이 있다. **«합의된 사실» 로 적지 않는다.**

### 🚨 그래서 이 등록이 고르는 것 — **ⓑ 팩터 모멘텀**

이유 셋. **결과를 보고 고른 것이 아니라 아래 논리로 고른다.**

1. **ⓐ 는 랩이 이미 져 봤고**(a2-factor-rot) 문헌도 갈린다. ⓑ 는 랩이 **한 번도 안 물었다.**
2. **ⓑ 는 «국면을 맞히지» 않는다.** 팩터 자신의 최근 수익만 본다 —
   랩이 열 번 중 아홉 번 실패한 것이 «국면 예측» 이고, 이것은 그 문제를 피한다.
3. **ⓐ 를 하려면 스타일 레그의 밸류에이션 총계가 필요한데**, 레그가 월마다 통째로
   바뀌어(회전 높음) Asness 외(2017)가 지적한 «보유가 안 남아 밸류에이션이 예측력을
   잃는» 조건에 정확히 해당한다.

⚠ **ⓐ 를 «나중에 해 보겠다» 고 적지 않는다.** 두 신호를 다 돌려 나은 쪽을 고르는 것이
이 랩이 금지한 일이다. **이 등록은 ⓑ 하나다.**

---

## 3. 규칙 — 계산 전에 못박는다

### 3-1. 자산 — 두 판을 **미리** 정한다

| 규칙 | 자산 | 고른 기준 |
|---|---|---|
| `x-style-fmom` | **스타일 8종**(`val·grow·hbeta·div·spmo·qvm·squal·size`) | STYLE8W 와 **같은 8종**. 선택이 없다 — **주 판정** |
| `x-style-fmom8` | **표준 8종**(`val·size·spmo·squal·div·lowvol·fcfy·netbuy`) | **표준 팩터 분류**(§3-1-b) — 성적이 아니다 |

### 🚨 3-1-a. 자산 선정을 **네 번 고쳤다.** 그 사실부터 적는다

| | 무엇 | 기준 | 왜 바꿨나 |
|---|---|---|---|
| 1차 | 16종 전부 | «있는 것을 다 넣는다» | — |
| 2차 | 9종 = 8 + `fcfy` | 🚨 **성적**(내가 16종 PIT 성적표를 보여 준 뒤 사용자가 지목) | 사용자 지시 |
| 3차 | 표준 7종 | 표준 팩터 분류(외부 기준) | 성적 기준을 외부 기준으로 **바꾸려고** |
| **4차(최종)** | **표준 8종** = 7 + `netbuy` | 같은 분류의 **투자(CMA) 축**을 마저 덮음 | 3차가 «표준을 덮었다» 면서 **FF5 의 다섯 중 하나를 비워 뒀기 때문** |

**넷 다 계산 전이다.** 그래도 **횟수 자체를 기록한다** — 읽는 사람이 할인할 수 있게.
⚠ **2차(9종)·3차(7종)는 폐기한다.** 만들지 않는다 — 판을 여러 개 돌려 나은 쪽을 고르지 않는다.
🚨 **네 번은 많다.** 자산 선정을 네 번 고친 등록의 «자산 수를 늘리니 좋아졌다» 는 결과는
**증거로 쓸 수 없다.** 그래서 §3-1-c 가 읽을 수 있는 것과 없는 것을 따로 적는다.

### 3-1-b. 표준 8종을 고른 규칙 — **성적을 쓰지 않는다**

**① 표준 팩터 축마다 하나씩 뽑는다.** 학계·업계에서 공통으로 세는 축이다:

| 표준 축 | 출처 | 랩의 후보 | **선택** |
|---|---|---|---|
| 가치 | Fama-French (1992) | `val` · `purevalue` | **`val`** |
| 규모 | Banz (1981) · FF (1993) | `size` | **`size`** |
| 모멘텀 | Jegadeesh-Titman (1993) | `spmo` · `mom` | **`spmo`** |
| 퀄리티 | Asness-Frazzini-Pedersen (2019) | `squal` · `qual` · `snqual` | **`squal`** |
| 배당 | MSCI 6대 팩터 | `div` | **`div`** |
| 저변동 | Ang 외 (2006) · Frazzini-Pedersen (2014) | `lowvol` | **`lowvol`** |
| 수익성 | Novy-Marx (2013) · FF5 (2015) | `fcfy`(대용) | **`fcfy`** |
| **투자(CMA)** | **FF5 (2015) · Fama-French (2008) 「Dissecting Anomalies」의 순발행** | `netbuy` · `buyback` | **`netbuy`** |

**①-2 🚨 투자 축의 둘 중 `netbuy` 를 고른 근거와, 그 근거의 약점**

| | PIT 샤프 | 무엇을 재나 |
|---|---|---|
| `netbuy` 주식수감소 | **0.94** | 발행 − 소각의 **순액**. 증자를 벌점으로 센다 |
| `buyback` 자사주매입 | 0.70 | 사들인 **총액**. 같은 해 증자로 다 게워 내도 안 보인다 |

**근거**: FF (2008) 이 투자 축의 대용으로 쓰는 것은 **순발행(net stock issues)** 이고
Daniel-Titman (2006) · Pontiff-Woodgate (2008) 도 같다. `buyback` 은 S&P 의 지수 설계이지
학술 축이 아니다. **그래서 `netbuy` 다.**

🚨 **약점을 그대로 적는다 — `netbuy` 가 `buyback` 보다 성적이 좋다**(0.94 vs 0.70).
**그리고 나는 이 표를 고르기 전에 이미 봤다.** 근거가 외부(FF 2008)라 해도,
성적이 반대였다면 내가 같은 결론에 갔을지 검증할 방법이 없다.
**그래서 이 선택은 «외부 근거» 와 «성적 일치» 가 겹친 자리로 기록한다.**
⚠ 반대 증거 하나는 적어 둔다 — **④의 `lowvol`(꼴찌)을 넣었다.** 한 등록 안에서
꼴찌를 넣고 상위를 골랐다면, 최소한 **일관되게 성적만 따른 것은 아니다.**

⚠ `netbuy` 도 CMA 의 **정확한 대용은 아니다** — CMA 는 총자산 증가율이고 `netbuy` 는
주식수다. 총자산 증가율 축(`x-agrow`)은 **레그가 없어 못 쓴다**(§7-8).

**② 동률은 «기존 8종에 있던 것을 유지» 로 깬다.** 가치·모멘텀·퀄리티가 그 경우이고,
셋 다 8종에 있던 쪽(`val`·`spmo`·`squal`)을 남겼다. **성적을 안 봤다.**

**③ 빠진 셋과 그 이유** — 전부 **축 중복이거나 축이 아니다**:

| 뺀 것 | 이유 |
|---|---|
| `qvm` 멀티팩터 | 여러 축의 **혼합**이라 표준 축 하나에 대응하지 않는다 |
| `hbeta` 고베타 | 저변동(BAB)의 **반대 방향**이다. 같은 축이므로 표준 쪽(`lowvol`)을 남긴다 |
| `grow` 성장 | 가치(HML)의 **공매도 다리**다. 성장 자체는 표준 팩터가 아니다 |
| `mom`·`qual`·`snqual`·`purevalue`·`buyback` | 이미 덮은 축의 **다른 판본**이다(§3-1-b② · ①-2) |

**④ 🚨 `lowvol` 은 16종 중 성적이 꼴찌다**(PIT 샤프 **−0.02**).
**그런데 넣는다.** 표준 축이고, 축이 비면 «표준을 덮었다» 는 말이 거짓이 되기 때문이다.
**이것이 이 선정이 성적 기준이 아니라는 가장 분명한 증거다.**

⚠ `fcfy` 는 2차에서 성적으로 지목된 것이지만, **3차에서는 «수익성 축의 대용» 이라는
근거로 다시 뽑힌다.** 결과는 같아도 **근거가 다르고, 근거가 판정을 읽는 법을 정한다.**
⚠ `fcfy` 가 수익성(RMW)의 **정확한 대용은 아니다** — RMW 는 매출총이익 기준이고
`fcfy` 는 잉여현금흐름이다. **대용이라고 적어 두고, 결과문서에도 그렇게 쓴다.**

### 3-1-b-2. 🚨 외부 지수 계열을 **전수로** 훑었다 — MSCI 6대 · S&P 스타일

사용자 질문 2026-09-21: *"s&p 스타일지수 중에서는 참고해서 추가할거 없어?"*
**이 절이 그 답이고, 등록서 안에 남기는 이유는 «안 넣은 것» 도 선택이기 때문이다.**

**⒜ MSCI 6대 — 전부 덮었다**

| MSCI | 랩 | |
|---|---|---|
| Value · Low Size · Momentum · Quality · Low Volatility · High Yield | `val`·`size`·`spmo`·`squal`·`lowvol`·`div` | **6/6 ✅** |

**⒝ S&P 스타일 계열 — 훑은 결과 «추가 하나, 나머지 없음»**

| S&P 지수 | 랩 후보 | 판정 |
|---|---|---|
| **S&P 500 Buyback** | `netbuy`·`buyback` | ✅ **이것이 8번째 축**(투자/발행). **S&P 계열이 채운 유일한 빈칸** |
| S&P 500 Pure Value / Pure Growth | `purevalue`·`grow` | ❌ 가치 축 **중복**. 순수판은 축이 아니라 **구성 방식**이다 |
| S&P 500 Enhanced Value | `val` | ❌ 중복 |
| S&P 500 Quality / Quality High Dividend | `squal`·`snqual`·`qual` | ❌ 중복(퀄리티는 `squal` 로 덮음) |
| S&P 500 Low Volatility / High Div Low Vol | `lowvol` | ❌ 중복 |
| S&P 500 Momentum | `spmo`·`mom` | ❌ 중복 |
| S&P 500 Dividend Aristocrats · High Dividend | `div` | ❌ 중복 |
| **S&P 500 Equal Weight** | — | ❌ **축이 아니다.** 가중 방식이고, 이 등록에서는 이미 **대조군 `ew`** 다(§3-5) |
| **S&P 500 Revenue-Weighted** | — | ❌ 가중 방식 |
| **S&P 500 GARP** | — | ❌ **혼합**(성장×가치×퀄리티). `qvm` 과 같은 이유로 뺀다 |
| S&P 500 Shareholder Yield / Dividend & Buyback | `div` + `netbuy` | ❌ **혼합**. 두 축을 이미 따로 갖고 있으므로 합친 것은 안 넣는다 |
| S&P 500 Multi-Factor | `qvm` | ❌ 혼합 |

> **결론: S&P 계열에서 새로 얻는 축은 「자사주·순발행」 하나뿐이고, 그것이 8번째다.**
> **나머지는 전부 ㉮ 이미 덮은 축이거나 ㉯ 가중 방식이거나 ㉰ 혼합이다.**
> ⚠ S&P 는 지수를 **팔 수 있는 단위**로 쪼갠다 — 순수판·고배당저변동·GARP 가 그 예다.
> **지수 개수는 축의 개수가 아니다.** 이 절의 요점이 그것이다.

**⒞ 아직 못 덮는 축 둘 — 명시한다**

| 축 | 왜 못 넣나 |
|---|---|
| 투자(CMA)의 **정공법** 총자산 증가율 | `x-agrow` 는 규칙이지 **스타일 레그가 아니다**. `style_pit.py` 를 고쳐야 한다 |
| **유동성** Amihud (2002) | 랩이 **영영 못 재는 자리**다 — 거래량을 쓰면 시점정확 다리가 막힌다(`x-amihud` t 6.84 가 검증 불가로 남은 이유) |

### 3-1-c. 그래서 두 판을 읽는 법

| 무엇 | 읽을 수 있나 |
|---|---|
| `fmom`(8종) 의 판정 | ✅ 자산이 STYLE8W 와 같고 선택이 없다 — **가장 깨끗하다** |
| `fmom8` 의 **유효 자산 수**(F8) | ✅ **이 등록의 진짜 질문이다** — 표준 축을 고루 덮으면 2.45 가 오르나 |
| `fmom8` 이 벤치를 넘는가(F5) | ✅ 문턱이 선택과 무관하게 서 있다 |
| `fmom8` 이 `fmom` 보다 나은가 | ⚠ 조심해서. 선정 기준은 외부지만 **내가 성적표를 이미 봤다.** 크기를 근거로 쓰지 않는다 |

⚠ **«나머지는 없애버려도 될듯» 을 «레그를 지운다» 로 읽지 않는다.**
`style_pit.json` 은 얼린 측정이고 그 안의 16종은 기록이다. **이 등록에 안 넣을 뿐이다.**

### 3-2. 신호 — Gupta·Kelly 의 시계열 팩터 모멘텀

형성월 `t` 말에, 각 스타일 레그의 **최근 12개월 누적수익에서 직전 1개월을 뺀 값**
(12-1)을 구한다. **그 레그의 12개월 수익 표준편차로 나눈다**(위험조정).

> **점수 = (레그의 t−12 ~ t−2 누적수익) ÷ (그 구간 월수익 표준편차)**

- 🚨 **직전 1개월을 건너뛴다** — 단기 반전 오염을 피하는 표준 관례이고,
  Gupta·Kelly 도 1개월 시차 수익으로 형성한다.
- 🚨 **레그의 PIT NAV 만 쓴다.** 소급 레그는 안 쓴다 — STYLE8W §0-1 이
  «소급 계열 위에 로테이션을 얹으면 중소형을 가장 좋은 자산으로 배운다.
  그 순위는 PIT 에서 성립하지 않는다» 고 적었다.

### 3-3. 배분 — **상위 절반 동일가중**

점수 상위 **N/2** 개(8종이면 **4** — 두 판 다 8종이라 같다)를 **동일가중**으로 든다.
나머지는 안 든다. **현금·채권을 넣지 않는다** — 그러면 «로테이션» 이 아니라
«타이밍» 이 섞여 무엇이 일했는지 못 가른다.

⚠ **상위 절반이라는 크기는 손잡이다.** 다른 크기(상위 1·2·3…)를 돌려 나은 쪽을
고르지 않는다. 절반을 고른 근거는 «롱온리에서 횡단면 신호를 쓰는 가장 흔한 형태» 하나다.

### 3-4. 시점·비용
- **월말 형성 · 1개월 보유.** 형성월 종가로 비중을 잡고 **다음 달부터** 수익을 센다.
- 왕복 **20bp**(STYLE8W 의 F4 와 같은 값 — 대조군과 같아야 한다).
- 🚨 **창은 10년 상한**(`build/maxyears.py` · `MAX_YEARS=10`).
  ⚠ 레그 자체가 2021-07 시작이라 실제 창은 그보다 짧다. **그 사실을 결과에 적는다.**

### 3-5. 대조군 — **STYLE8W 의 `ew`**
같은 자산·같은 기간·같은 비용의 **8종 동일가중**. `fmom8` 의 대조군은 **표준 8종 동일가중**이다.
⚠ 벤치(S&P 500)도 함께 싣되, **판정은 `ew` 대비로 한다**(STYLE8W 등록서의 지시).

---

## 4. 결과문서에 반드시 실을 것

1. **유효 자산 수** — 8종과 표준 8종 각각(상관행렬 고유값). §1-① 이 이 등록의 전제다
2. **명단 겹침** — 스타일 레그끼리의 평균 자카드
3. 성적 — `fmom` · `fmom8` · `ew8` · `ew8s` · 벤치, 비용 전후
4. **회전율** — 상위 절반이 매달 얼마나 바뀌나
5. **신호의 지속성** — 점수의 월간 자기상관
   (RUNS2 의 교훈: 0.7 아래면 회전 장치를 기대하지 않는다)
6. 셔플 200회 · 앞뒤 절반 · 국면(변동성 3분위)

## 5. 기각 조건 — 각 규칙에 따로 건다

| | 조건 |
|---|---|
| **F1** | `ew` 대비 **초과가 ≤ 0**(비용 후) |
| **F2** | `ew` 대비 초과의 **t < 1.5**(STYLE8W F2 와 같은 문턱) |
| **F3** | **셔플 200회 백분위 < 95** — 같은 크기 무작위 스타일 뽑기 |
| **F4** | 왕복 20bp 뒤 **F1 이 뒤집힘** |
| **F5** | **벤치(S&P 500)를 샤프에서 못 넘음** — 🚨 STYLE8W 에서 넷 다 못 넘었다. 로테이션이 그것도 못 고치면 이 계열은 끝이다 |
| **F6** | 월 회전율 **> 50%** (상위 절반이 매달 절반 넘게 바뀌면 신호가 아니라 잡음) |
| **F7** | 표본 **뒤 절반에서 부호 반전** |

**`fmom8` 전용 추가 조건**

| | 조건 |
|---|---|
| **F8** | 표준 8종의 **유효 자산 수가 8종의 2.45 보다 안 오름** — 표준 축을 덮은 이유가 사라진다 |

⚠ F5 가 이 등록에서 가장 중요하다. STYLE8W 가 «벤치가 넷을 다 이겼다» 로 끝났으므로,
**로테이션이 `ew` 를 이겨도 벤치를 못 이기면 쓸 데가 없다.**

## 6. 예측 — 계산 전에 적는다

| | 예측 | 왜 |
|---|---|---|
| **P1** | **`fmom` 이 F1 또는 F2 에 걸린다** | 유효 자산 2.45 개에서 상위 4개를 고르는 것은 «거의 전부를 고르는 것» 이다 |
| **P2** | **F5 에 걸린다**(벤치를 못 넘는다) | `ew` 가 이미 벤치에 졌고, 로테이션이 그 격차(샤프 0.89 vs 0.92)를 메울 만큼 크지 않을 것 |
| **P3** | 표준 8종의 유효 자산 수가 **3.0 ~ 4.5 사이**일 것(2.45 보다 오른다) | 중복 셋(`qvm`·`hbeta`·`grow`)을 빼고 서로 다른 축(`lowvol`·`fcfy`·`netbuy`)을 넣었다. 자산 수는 8로 같은데 겹침만 줄었으니, 이 비교는 **개수가 아니라 축의 다양성만** 바뀐 깨끗한 대조다 |
| **P4** | `fmom8` 이 `fmom` 보다 **나을 것** | 유효 자산 수가 오르면 로테이션이 일할 여지가 커진다. ⚠ 다만 `lowvol`(성적 꼴찌)이 들어가 있어 **단독 성적은 낮아질 수도** 있다 — 그래서 이 예측은 반반으로 본다 |
| **P5** | 점수의 월간 자기상관이 **0.5 ~ 0.8** | 12-1 누적은 한 달에 1/11 만 바뀐다 |
| **P6** | 회전율이 **월 20 ~ 40%** | 위 자기상관이면 상위 절반의 순위가 그 정도 흔들린다 |

⚠ **P1·P2 가 맞으면 기각이고, 그것이 내가 보는 그림이다.**
그때의 결론은 «스타일 8종은 로테이션으로도 안 된다» 이고,
**STYLE8W 가 열어 둔 질문이 닫힌다.**

## 7. 안 할 것

1. **결과를 보고 자산 수(8/7)·선택 크기(상위 절반)·신호(12-1)·주기(월말)를 바꾸지 않는다.**
2. **밸류에이션 신호(ⓐ 진영)를 같이 돌려 나은 쪽을 고르지 않는다**(§2 의 결정).
3. **현금·채권 다리를 넣지 않는다.** 로테이션과 타이밍을 섞지 않는다.
4. **소급 레그를 쓰지 않는다**(STYLE8W §0-1).
5. **`fmom` 과 `fmom8` 중 나은 쪽을 «채택» 이라 부르지 않는다.** 각자 판정한다.
6. **16종판·9종판·7종판을 만들지 않는다**(§3-1-a). 판을 여러 개 돌려 나은 쪽을 고르지 않는다.
7. **기각이어도 지우지 않는다** — 원장에 남긴다.

## 8. 계산 전 증명

이 문서를 **계산 전에 커밋**한다.
구현: `build/style_fmom.py` → `data/_style_fmom.json`
