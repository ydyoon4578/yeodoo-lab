# 사전등록 — 배치 R: 종목 고유 악재 걸러내기 단계의 기전 검정 (1차 후보 R1 · R2 · 측정 R3 · 사건 수 R5) · ⟨TBD: 등록 날짜 YYYY-MM-DD⟩

> **뼈대(등록 전 초안).** `⟨TBD: …⟩` 칸은 자료 → F0 → P0 → 해시 커밋 순서가 끝난 뒤 채운다. 러너(`build/r_run.py`)는 등록 커밋 판에
> `⟨TBD` 가 하나라도 남아 있으면 굽지 않는다. 등록 때 이 파일 이름의 `2026-09-2x` 를 등록 날짜로 바꾸고, 러너의 `PREREG` · `PREREG_CARDS` · `RESULT`
> 상수도 같은 커밋에서 고친다(이름이 자리표시자인 동안 러너는 굽기를 거부한다).

사용자 목표(메모리 · `C:\Project\fund_reports\펀드전략_작성원칙.md`): 시그널로 전략을 바꿔 **꾸준히** 지수를 이기고, **급등 · 급락 양쪽**에서 이긴다 ·
하락 구간 방어가 우선 · 섞지 말고 **스텝을 더해** 단점을 보완한다 · 모든 전략에 근본 이유.
배치 R 은 EG30(급등형 · 이름 붙은 급락 0/6)에 **급락형 단계**(종목 고유 악재가 있는 이름을 빼는 단계)를 더하려 한다. 양면 관문은 쌓은 규칙에 건다.

🚨 **이 문서는 어떤 카드의 표지와 미래 수익의 관계도 본 사람이 없을 때 커밋한다.** 등록 전에 계산된 것은 자료 · 밀도 · 커버리지 F0(수익 없음)와
P0 의 σ(무작위 플래그 대 실제 수익의 SD 만 · 위약 평균은 저장하지도 찍지도 않았다)뿐이다. 카드 원문(slate · data_build_plan · batch_design_changes)은
같은 커밋의 `build/PREREG-⟨TBD: 날짜⟩-RBATCH-CARDS.md` 에 그대로 얼린다.
**우선순위(하나)**: 규칙 = §13(구현 해석 · 선언) > 카드 원문 > §3 요약 · 주 통계 · 관문 · 다중성 · 판정 = §4~§7 = 얼린 `build/r_run.py` 와 `build/r_stagem.py` —
글과 코드가 어긋나면 **얼린 코드가 돌고** 그 어긋남은 결과 문서에 «등록 오류» 로 적는다.
🚨 시도가 약 750회 쌓였다. 계속 시도하면 좋은 결과가 우연으로 반드시 나온다 — 그래서 1차를 최대 둘로 묶고, m 을 등록 커밋에 고정하고,
표본 안 통과를 채택으로 보지 않으며, 떨어진 규칙을 고쳐 다시 굽지 않는다.

## 0. 이미 본 것 (오염 공개)

- **EG30(V0 · 공개 판)**: 총수익 기준 연 +0.76% · TE 0.93% · IR 0.82 · 하락월(S&P 500 PR < 0 · 39개월) 평균 −0.075%p · 이긴 해 8/11 · 급락 0/6 · 급등 5/6.
- **배치 Q**(PREREG-2026-09-25-QBATCH · 기각): 표본 안 유의 0 · 전방 진입 0 · 가장 가까운 Q07(EG30 + 코퓰러 LTD) 이긴 해 10/11 · p 0.14.
  양쪽을 다 이긴 규칙은 Q05 하나였고 효과 크기가 0 근처였다.
- **2016-09 ~ 2026-08 창은 랩이 여러 번 들여다본 창이다** — 표본 안 통과는 채택이 아니다(§10 전방만).
- **랩은 내부자 자료 · 10-Q 텍스트로 수익을 계산한 적이 없다**(카드 원문 why). 8-K 사건(R3 · R5)도 표지로 수익을 잰 적이 없다.
- **등록 전에 본 수(수익 없음)**: R1 월 매도 발행사 수(2016-07/08/09 190/315/216 · 2025-07/08/09 172/264/212 · 강제 매도 제외 뒤 185/312/214 · 171/263/210) ·
  §F 커버리지 실측(2016-08..2026-07 멤버-월 62,703 중 4,483 · 7.1% · 104종 가격 없음 · 2016년 15.2% → 2026년 0.2%) · §A0 지도 F0 ·
  밀도 · 커버리지 F0 표(§8) · P0 표(§6).
- **원 계획의 P0 0.15 는 계산이 아니었다** — 계획 자신의 입력으로 다시 계산하면 약 0.12 다. 그 값은 버리고 §6 의 식으로 고정했다.
- **등록 전에 돌린 것**: 조사 · 심판 · 비평 워크플로(성과 계산 금지) · 자료 빌드(§A0 · §A · §B · §C · §D) · 파서 손 라벨 검증 · 밀도 · F0 ·
  P0(위약 SD) · 러너 합성 시험(`r_run.py --selftest` ⟨TBD: 통과 수⟩) · 눈가린 연기 시험(`--smoke-real` — Stage M 은 y 를 잡음으로 바꾼 패널 ·
  Stage S 는 실제 세계 위 **합성 표지**로 `r_stages.run` 을 blind_smoke 에 · 값은 버림 · ⟨TBD: 결과 — 예외 없음 · 초⟩) · 등록 대조(`--check-reg` · `--check-reg --rebuild` ⟨TBD⟩).

## 0b. F0 전에 정한 것 (2026-09-26 · 오케스트레이터가 이 라운드 전에 정했다 · 되묻지 않는다)

F0(커버리지 · 밀도)와 P0 를 세기 **전에** 정한 규칙이다. 결과(T · 밀도 · P0)가 어떻게 나오든 이 규칙대로 적는다.

- **커버리지 F0 세계** = Stage M 세계 = `r_stagem.build_panel` 의 분모 — 그달 S&P 500 ∪ NASDAQ 100 멤버 가운데 비금융 · FPI 아님
  (지도의 등록 표지 `fpi_q` = tm 7번 칸 · `_issuer_map.json fpi_registered.tm_index` — 모든 읽개가 이 칸을 읽는다) · §A0 지도 F0 위반 멤버-월 아님.
- **덮인 멤버-월** = 월말 가격 ∧ 시총(주식수 관측 나이 ≤ 550일 = 랩 `TTM_STALE_DAYS` ∧ 그 관측의 펀드 파일 CIK 가 그달 §A0 CIK 집합에 있음) ∧
  장부가(≤ 0 허용 · 같은 나이 · 같은 CIK 규칙) ∧ 12-2 모멘텀. 펀드 파일 CIK = 파일의 CIK 구간(`ciks` — 선행 CIK 를 잇는 빌드가 싣는다 ·
  별칭 `cik_ranges`)이 그 관측 날짜를 덮는 CIK 들 · 구간이 없거나 덮지 않으면 «출처 모름» = 불일치. 회귀 행도 같은 시총 · 장부가를 쓴다.
- **달 판정** = 덮인 몫 개수 ≥ 0.90 ∧ 시총 ≥ 0.95 · 시총 가중 = 자기 시총 → 12개월 안 마지막 자기 시총 → 그달 같은 지수(S&P 500 멤버면
  S&P 500 · 아니면 NASDAQ 100) 자기 시총 중앙값. **T = 2016-08..2026-07 가운데 통과한 달 수 — 나온 T 가 최종이다.**
- **보유월 수익 y** — 가격 계열이 보유월 말 전에 끝나고(끝 뒤 5 거래일 안에 다시 서지 않음) 회사가 계속 거래됐으면(지수만 떠남 · 이름만 바뀜)
  **y 결측**(마지막 가격 아님). 마지막 가격 규칙은 실제 상장폐지(인수 · 파산 · 비공개화)에만 남긴다. 이름마다 공개 지식으로 정했고 목록은
  `data/_r_ytrunc.json`(`r_stagem --ytrunc-scan` 이 대상을 찾는다 · 판정 없는 끝은 결측으로 두고 등록 러너는 멈춘다). 월말 행만 빠진
  «끊김»(DB 2018-05-31 · 2025-06-26..30 등)은 달 안 마지막 값 수익(1~3일 이른 끝 · 선언).
- **10-Q(R2)** — 등록 형태 판 = **v3**(§C 파서 라운드 2 · 엄격 형태 정확도 0.95 · 관례 0.96 · 모집단 가중 0.931) · R2 F0 분리 실패 =
  **split_fail + no_1a** · Q1 규칙 = **«B-B 짝은 변경 없음»**(유효 짝의 앞 · 지금 형태가 모두 B 면 SimRF = 1 로 두고 경계 분포에도 1 로 넣어
  경계를 다시 잰다 · 그 짝은 Q1 이 될 수 없다) — `r_r2flags.REG_SHAPE_VER` · `REG_Q1_RULE`.
- **내부자(R1)** — 등록 1차는 **제출된 그대로의 값**(4/A 가 고쳐도 원 행 sh · px · usd 는 바꾸지 않는다 · 원 가용월). 고친 값(`shc · pxc · usdc`)은
  **선언된 민감도 T1C**(T1 과 같고 고친 금액 · 엔진 시도 `R1.t1c`)만 쓴다. 거래일만 고친 4/A(DATE_OF_ORIG_SUB 의 제출 행과 보고자 · 코드 ·
  수량 · 단가가 같다)는 새 사건이 아니다(cnt 0 · 원 행 가용일 그대로 · 고친 거래일은 분류 이력에만 — `r_r1_flags` 선언 q2).
- **P0 바닥(META-2 문헌 t 옮기기)** — **P0 = min(P0_σ, P0_lit)**. P0_σ = §6 식(σ_plan 규칙) · P0_lit = q·π_lit + (1−q)·α₁ ·
  π_lit = 1 − Φ(2.27 − t_alt_lit) · **t_alt_lit = 0.5 · t_lit · √(T / T_lit)**(T = P0 카드 T · `r_p0.LIT` · 러너가 다시 푼다).
  - R1: CMP «Decoding Inside Information» 표 IX 열 (2) 대형주 기회주의 매도 −0.55(**t 3.44**) · **T_lit 264**(본문 «1986-01..2007-12 · 22년» ·
    수익 검정 표는 1989–2007 = 228개월로 적힌 곳이 많다 — 긴 쪽 · 보수) — t 확인(NBER w16454) · 달 수 부분 확인.
  - R2: Lazy Prices 표 A-15 Q1 5요인 동일가중 −0.80(**t 2.22**) · 가치가중 −0.96(t 2.34 · 작은 쪽을 쓴다) · **T_lit 240**(논문 표본 1995–2014) —
    t 확인(NBER w25084) · 달 수 **미확인**(표에 기간이 없고 10-Q Item 1A 는 2006 부터라 실제 짝 표본은 ≤ 108개월로 보인다 — 긴 쪽 · 보수).
  - 결과(식에서 바로 나온다 · 수익 없음): T = 120 에서 P0_lit = 0.061(R1) · 0.035(R2) — 관문 0.15 를 넘으려면 T ≥ 323(R1) · 704(R2)개월이다.
    **T ≤ 120 이면 σ 와 무관하게 두 카드 모두 P0 < 0.15 → m = 0**(§6 · §7 의 «측정만» — 미리 정한 정상 결과).

## 1. 공통 틀

**Stage M(기전 검정 · 표본 안 한 번 · `build/r_stagem.py`)**
- 세계: 그달 말 시점정확 S&P 500 ∪ NASDAQ 100(pit_panel.union_members) · 시점정확 GICS 비금융 · §A0 발행사 지도로 그룹이 풀린 멤버-월 ·
  FPI(직전 연차보고 20-F · 40-F) 제외 · 그룹당 한 종목(시총이 큰 쪽) · 가격 · 시총이 선 행 · 커버리지 F0 와 지도 F0 를 넘은 달만.
- 신호월 t = 2016-08 ~ 2026-07(보유월 t+1 = 2016-09 ~ 2026-08 · T 명목 120).
- 회귀: 월별 횡단면 OLS `r_{i,t+1}(%) ~ 1 + 표지 + log 시총 + log B/M(B/M ≤ 0 · 결측 → 결측 더미) + r_t + r_{t−12..t−2} + GICS 섹터 더미` ·
  FWL 로 통제를 빼고 표지 계수만 푼다 · R1 · R3 는 k = 0, 1, 2 세 회귀의 계수를 달마다 평균(JT) · R2 는 보유 창이 더미 안에 있어 단일 회귀.
- 통제 자료: 시총 = pit 가격 × pit_panel._shares(fx + fx_pit · 90일 지연 · AUDIT-2026-09-20-SHARES2 분할 되맞춤 판 — 러너가 시총 핀으로 단언) ·
  B/M = tech_backtest 최신 제출본(재작성 누수 · 선언) · 모멘텀 · 반전 = stocks.json + pit_px.json · 섹터 = pit_gics_sectors.
- 보유월 수익은 pit_panel.month_rows 규약(달 중간 상장폐지 = 마지막 가격 · 다음 달 가격이 하나도 없으면 0) — 편향 방향은 결과 문서에 적는다.

**Stage S(EG30 단계 · 표본 안은 측정만 · 판정은 전방)**
- V0 = EG30 공개 판. 분기 편입일 r 마다 Eg 상위 30 가운데 표지 활성 이름을 빼고 표지 비활성 Eg 차순위로 채워 30종 ·
  비중은 V0 규칙(qg_lab.World.weights(EG_BASE) · 시총가중 · 20% 상한) · 편입 사이는 바꾸지 않는다. 카드 문구의 «금융은 지수 비중» 은 V0 에 없는
  규칙이고 V0 에 금융이 한 번도 들지 않아(snap_wt 0/1230 이름-편입) 적용하지 않는다(`r_stages` 선언 ①). 구현은 `build/r_stages.py`(선언 ①~⑫ 는 §13).
- 틀은 `build/qbatch_core.py`(90% SPY TR + 10% 슬리브 · 월말 되돌림 · 편도 10bp · 20bp 행을 함께 싣는다).
- 배치 Q 에서 빌린 «상위 40 · 최대 10 · 강도순» 모양은 쓰지 않는다 — 교체는 Stage M 더미를 그대로 옮긴 것이라 새 강도 모수가 없다.

## 2. 자료 고정

- **P1 가격**: 커밋 `⟨TBD: snap⟩` 의 stocks.json · pit_px.json · sd/ — 두 격자가 같고 검증을 통과한 가장 이른 커밋(§F · HEAD e19d13ad 는 4,457 대 4,459일로 불일치).
- **P2 가격 밖**: 커밋 `⟨TBD: base⟩` 의 bench_px · rf_monthly · index_history · index_ledger · pit_universe · pit_reuse · fx · fx_pit · assets · splits · shares_yf · cik_map.
- **P3(등록 커밋에 얼린다)**: pit_gics_sectors · _eg_q5_scores_pitgics · _eg_q5_scores_pitgics_pre · _eg_q5_scores · mech_episodes · _eg30plus.
- **새 자료(등록 커밋에 blob 으로 얼린다 · 러너가 RBATCH_DATA 쪽을 대조)**:

| 파일 | 무엇 | sha256(LF) |
|---|---|---|
| data/_issuer_map.json | §A0 날짜 인식 발행사 지도 + §B Section 16(FPI) 표지 | ⟨TBD⟩ |
| data/_issuer_map_manifest.json | §A0 SEC 원본 고정표(submissions · browse) | ⟨TBD⟩ |
| data/_issuer_map_manual.json | 수작업 검증 목록 | ⟨TBD⟩ |
| data/_ins_pit/manifest.json | DERA 분기 ZIP 2011Q1~2026Q2 고정표(sha256 · 크기 · Last-Modified) | ⟨TBD⟩ |
| data/_ins_pit/cikmonth.json | §A 그룹 × 가용월 집계(O · R · U · N · all · bO · bR) + 쌍둥이 칸(t1_os3 · t2_* · t4_* · t6_* — T1~T6 은 이 한 파일의 칸이다 · 따로 파일 없음) | ⟨TBD⟩ |
| data/_ins_pit/routine.json | CMP 분류(1월 첫 거래일) | ⟨TBD⟩ |
| data/_ins_pit/daily_check.json | §G 일간 XML 대 DERA 일치 시험(2026Q2) | ⟨TBD⟩ |
| data/_sub_pit.json.gz | §B EDGAR submissions PIT 패널 | ⟨TBD⟩ |
| data/_tenq_rf.json | §C 10-Q 위험요인 절 특징(문서별) | ⟨TBD⟩ |
| data/_tenq_rf_validation.json | §C 파서 검증(손 라벨 100 · LM 대조 · ix:header) | ⟨TBD⟩ |
| data/_ev_flags.json | §D R3 FL · R5 경보 그룹-월 | ⟨TBD⟩ |
| data/_r_p0_counts.json · data/_r_p0.json | P0 개수 표 · P0 문서(result_sha ⟨TBD⟩) | ⟨TBD⟩ |
| data/_r_f0.json | 러너 F0 문서(`r_run.py --f0` · 수익 없음) | ⟨TBD⟩ |

- **선택 입력(있으면 읽힌다 — 등록 커밋과 로컬 모두에 없으면 «없음» · 한쪽에만 있거나 다르면 굽지 않는다)**: data/_px_raw.json(시총 원 종가 · 없으면 배당조정 판 — 선언)
  ⟨TBD: 있음/없음 · sha256⟩ · data/_t401.json(R5 4.01 본문) ⟨TBD⟩ · data/_r_coverage.json(r_stages · r_p0_adapt 의 §F 달 관문) ⟨TBD⟩.
- **굽기에 없어야 하는 파일**: data/_r_flags.json(덮어쓰기 표지 — 정식 빌더를 가린다 · r_stages · r_p0_adapt 옵트인). 있으면 러너가 굽지 않는다.
- 파생 파일의 고정표 링크(예: `_issuer_map.json` pins.ins_manifest · im_manifest · manual · script)는 러너가 굽기 때 **모두** 확인한다(확인 못 하면 굽지 않는다).
  원본(저장소 밖 RBATCH_RAW)이 있으면 고정표 sha256 을 다시 잰다 — 어긋나면 멈춘다.
- DERA 판: 과거 분기는 2022년에 재추출됐고 2025-07 에 2023~2025판에 AFF10B5ONE 이 소급 추가됐다 — 고정표 해시가 바뀌면 빌드를 멈춘다.
- SEC 요청: 모든 요청은 `build/edgar.py`(초당 8회 · gzip · 백오프) · SEC_UA 를 명시적으로 설정해야 돈다(기본값을 조용히 쓰지 않는다) ·
  빌드 전 index.json 1건 · submissions 1건의 HTTP 200 과 gzip 을 확인했다(⟨TBD: 기록 경로⟩). 사용자 이메일은 어디에도 쓰지 않았다.
- numpy 2.5.3 · scipy 1.18.1 · PYTHONHASHSEED=0 · OPENBLAS_NUM_THREADS=1.

## 3. 카드 (규칙 전문은 CARDS 파일 · 여기는 근본 이유와 한 줄 규칙)

| 코드 | 자리 | 이름 | 근본 이유(요약) | 규칙 한 줄 | 주 대조 · 측정 |
|---|---|---|---|---|---|
| **R1-OPPSELL** | 1차 후보(P0 관문) · 내부자 가족 | 기회주의 내부자 매도 걸러내기 | 매도의 대부분은 분산 · 세금 · 생활자금이고 해마다 같은 달에 되풀이된다(루틴 · 정보 없음). 루틴 패턴이 없는 «기회주의» 매도는 곧 나올 나쁜 소식을 먼저 반영하고 시장은 공시 뒤에도 덜 반응한다(CMP 2012 Table IX (2) −0.55%/월 · t −3.44 · 기준은 매수만 있던 회사-월) | Form 4/4A NONDERIV S(A/D = D) · 금액 하한 없음 · 10b5-1 로 빼지 않음 · 강제 매도 각주 행 제외 · 모든 내부자 · 1월 첫 거래일 3년 분류 · 사건월 = 가용월 · OS_{t−k} + RS_{t−k} · JT k = 0..2 | RS 계수 · 분류 없는 전체 매도 · 관문 ii · iii 판 · OS × Eg상위40 · 쌍둥이 T1~T6 · Stage S C1~C4 · 위약 1000 |
| **R2-LAZYRF** | 1차 후보(P0 관문) · 텍스트 가족 | 10-Q 위험요인 절 대규모 개정 걸러내기 | 경영진은 위험요인 절을 거의 그대로 베껴 쓴다 — 크게 고쳐 쓴 분기의 새 문장은 대개 나쁜 소식(늦게 알리면 집단소송 위험)이고 투자자는 읽지 않는다(공시일 반응 0 · 뒤에 실적 · 뉴스 · 파산으로 반영 · 되돌리지 않음 · Lazy Prices 표 A-15 Q1 −0.80%/월) | 10-Q Part II Item 1A 직전 분기 짝(70~200일) · Sim_Cosine · 형태 전환 결측 · 전년도 분포 하위 20% → Q1 · CH_active = t−2..t 에 Q1 · 결측 더미 | 결측 더미 · 관문 i · ii 판 · SimDoc 대조 · iXBRL 전환 · 2020-03~08 진단 · Stage S C1~C5 |
| R3-8KNE | 측정만(가족 밖) | 비실적 8-K 급반응 종목 제외 | 해석이 어려운 비실적 8-K 는 의견 차이를 키우고 공매도 제약과 만나 가격이 비싸게 남았다가 다음 실적 전후에 걷힌다(Li · Tan 2022 초록 · Miller 1977) — S&P 500 에서는 약해야 한다 | 2.02 없는 8-K · |CAR[a−1, a+1]| ≥ 5% → 창 끝 뒤 첫 월말부터 다음 2.02 뒤 첫 월말까지 · FL_{t−k} JT | 긴 창 쌍둥이 · 좋은/나쁜 뉴스 · ±5일 오염 창 · 위약 · 고베타 |
| R5-ALARM | 사건 수만(전방 기록 팔) | 회계 신뢰 · 부실 경보 위생 단계 | 정기보고 지연 · 재무제표 신뢰 불가 · 감사인 사임 · 상장유지 경고 · 채무 가속 · 손상 · 구조조정은 연달아 온다(Huang · Scholz 2012 · Menon · Williams 2008 · Lerman · Livnat) | (a) 12개월: NT 10-K/Q · 4.02 · 4.01 사임 · 3.01 · (b) 90일: 1.03 · 2.04 · 2.05 · 2.06 · 5.01 | 표본 안: 이름-월 수 · 업종 분포만(수익 없음 · 시도 수 밖) |

R4(집계 NPR)는 뺐다(L&L 3개월 지평 예측력 거의 없음 · 대형주 집계 차이 5% 비유의 · 갈림길을 만든다). SK3 · SK4 · SK5 · SK9 · INS1 · TXT2 · 연간 Lazy Prices 사양은 만들지 않는다(카드 원문 10).

## 4. 주 통계 (Stage M · `r_run.primary_stat`)

- R1: 평균 γ_OS(JT) 월 계열의 NW(3) t · 한쪽 H1 γ_OS < 0.
- R2: 평균 γ_CH 월 계열의 NW(3) t · 한쪽 H1 γ_CH < 0.
- R3: 평균 γ_FL(JT) — 보고만(가족 밖).
- **NW(3) t 는 달력 위치를 지킨다(한 정의 · 엔진 `r_stagem.summarize` = 러너 `r_run.primary_stat`)**: 계수가 선 달이 달력으로 이어지면
  `eg30plus.nw_t` 그대로 · 빈 달(흩어진 커버리지 F0 실패 달 · 달 회귀의 자유도 < 10 으로 건너뛴 달 · 주 표지가 어느 JT 시차에서 0 이라 γ 가
  서지 않은 달)이 끼면 `r_stagem.nw_t_gap`(같은 바틀렛 식 · n = 선 달 수 · 빈 달의 편차 0 · 시차 L 곱은 달력으로 L 달 떨어진 두 달이 모두 선
  쌍만)을 주 통계로 쓴다. 압축 계열 값(`nw_t_compressed`)과 빈 달 수(`gaps`)를 함께 싣는다(보고만).
- 한쪽 p = P(t(T−1) ≤ t) · T = 계수가 선 달 수(커버리지 F0 를 넘은 신호월 가운데) — T = 120 이면 t(119). t 가 서지 않으면 p = 1(닫힌 실패).
- 러너는 엔진의 요약(T · 평균 · NW t · p · 빈 달 수)을 같은 정의로 다시 계산해 1e-9 로 대조하고 다르면 멈춘다.
- 미리 등록한 민감도(측정): 2020-07 이후 달만 쓴 γ(러너 `sens2020`). 2014-07..2016-08 창은 편출 가격이 2014-06 까지 복구될 때만 열 수 있고
  지금 러너에는 없다 — 열려면 등록 전에 러너에 더하고 여기에 적는다 ⟨TBD: 열었는가⟩.

## 5. 관문 (계산 전 고정 · 모두 점 추정 · `r_run.gates_r1` · `gates_r2`)

- **R1** (i) 같은 달 γ_OS − γ_RS 평균 < 0(분류가 일한다) — 월 RS 종목 수 중앙값 ≥ 10 이 아니면 거짓(닫힌 실패) ·
  (ii) 52주 고점 괴리 + 12개월 수익을 더 통제해도 γ_OS < 0(역발상 재포장이 아니다) · (iii) 시총가중 WLS 에서도 γ_OS < 0(단계가 시총가중이다).
- **R2** (i) 시총가중 WLS 에서도 γ_CH < 0 · (ii) Δlog(Item 1A 단어수)를 통제에 더해도 γ_CH < 0(길이만의 효과가 아니다 · CMN 표 VI).
  보고만: 결측 더미 계수 < γ_CH 이면 결과 문서에 «분리 실패 오염».
- 측정으로 싣는다: 표지 × (Eg 상위 40 소속) 상호작용 · 모멘텀 빼기와의 자카드(등록 전 · ⟨TBD⟩).
- 관문 값이 불리언이 아니면 러너가 멈춘다. 엔진의 관문과 러너의 관문이 다르면 멈춘다(엔진에 RS 밀도 F0 를 넘겨 F0 조건을 건 뒤의 값끼리 · 오염 보고 포함).

## 6. 다중성 · 1차 자격 (P0 · Holm)

- **P0 = q·π + (1−q)·α₁** · q = 0.4 · α₁ = 0.0125 · π = 1 − Φ(2.27 − 0.5·|γ_lit|·√T / σ_plan) · σ_plan = max(σ_analytic, 칸 맞춘 위약 SD 의 90분위 · 200회).
  0.15 관문 ⇔ π ≥ 0.356 ⇔ t_alt ≥ 1.90. 규칙을 결과에 맞춰 완화하지 않는다.
- 자격 = P0 ≥ 0.15 ∧ T ≥ 84 ∧ 카드 밀도 F0 ∧ 카드 외부 F0(R2: §C 파서 · 연간 짝 ≥ 900 · 분리 실패 ≤ 20%). 같은 자료 가족에는 1차를 하나만.
- 자격의 T 는 **P0 카드 T** 다(F0 쓸 달 − P0 가 뺀 «원천 정의 밖» · «주 표지 0» 달 · `r_p0.card_months`). 러너는 P0 카드 T = F0 T − 그 뺀 달
  이어야 하고 P0 가 달 관문 · 행 부족으로 F0 쓸 달을 빼지 않았어야 굽는다(카드마다 · 가족 안팎 모두). P0 · T 관문은 r_p0 의 p0_pass · T_pass
  (반올림 전 값)를 따르고, 러너가 σ_plan · t_alt · π · P0 를 문서 칸에서 다시 풀어 1e-6(t_alt · π 는 5e-6) 안에서 같아야 한다.

| 카드 | γ_lit | T | σ_analytic | σ_placebo(P90) | σ_plan(묶음) | 문턱 σ | t_alt | π | P0 | 자격 |
|---|---|---|---|---|---|---|---|---|---|---|
| R1-OPPSELL | −0.55 | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD: T=120 이면 1.58⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ |
| R2-LAZYRF | −0.80 | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD: T=120 이면 2.30⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ |

- **Holm 가족 크기 m = ⟨TBD: 0 · 1 · 2⟩ · 가족 = ⟨TBD⟩** — 이 커밋에 고정한다. 등록 뒤 어떤 사건에도 m 은 바뀌지 않는다(R3 조건부 승격 없음 ·
  가족 안 카드는 p 가 서지 않아도 p = 1 로 남는다). 러너의 REG · data/_r_p0.json(holm.m · members · result_sha) · data/_r_f0.json 이 같아야 굽는다.
- 한쪽 가족 α 0.025 · 작은 p 부터 α/(m − j): m = 2 → t(119) 2.27 → 1.98 · m = 1 → 1.98 · m = 0 → 1차 없음(둘 다 측정만 — 미리 정한 정상 결과).
- DSR 은 이 배치의 판정에 쓰지 않는다. 시도 수: 이 배치 ⟨TBD: 러너 trials.batch 예상 약 38 = Stage M 25(R1 12 · R2 7 · R3 6) + Stage S 13⟩ ·
  랩 누적 ⟨TBD: 약 716⟩ → 약 754. 시도 수는 엔진 · 러너 시도 목록의 «돈 줄» 로 센다(T2 전후 분할 · 20bp · PR 행은 한 시도 안) · 못 돌린 시도는
  사유와 함께 결과에 싣는다(선언한 것: R1 · R2 2014-07 넓힌 창 · R3 Stage S 세 팔 — §13).

## 7. 판정 라벨

- 가족 밖(P0 < 0.15 · T < 84 · F0) → **«측정만»**(사유를 싣는다) · 단계 팔은 측정 팔.
- 가족 안 · Holm 통과 ∧ 관문 전부 → **«Stage M 통과 — 단계는 전방 판정»** · Holm 통과 ∧ 관문 실패 → «기각 — 관문 … 미달» · Holm 실패 → «기각».
- R3 → «측정만(Holm 가족 밖 · 승격 없음)» · R5 → «사건 수만(수익 계산 없음 · 시도 수에 넣지 않는다)».
- 단계 팔의 **채택 경로**는 Stage M 통과 ∧ Stage S F0(편입당 교체 수 중앙값 2~15) 참일 때만 연다. R1 팔은 §G 일치 시험, R2 팔은 §C 파서 검증을
  통과해야 전방에서 연다. 그 밖은 측정 팔.
- RESULT 머리 줄: `풀카드: 없음` · `판정: Stage M 통과(…) — 채택 아님 · 단계는 전방 판정` / `판정: 측정만`(m = 0) / `판정: 기각` · `규칙: 없음`.

## 8. F0 표 (수익 없음 · 등록 전 커밋 · 러너가 굽기 때 같은 판으로 다시 세어 바이트 수준으로 같아야 한다)

**§F 커버리지(개수 ≥ 0.90 · 시총 ≥ 0.95) · §A0 지도(위반율 ≤ 2%)**

| 연도 | 멤버-월 | 커버 개수(중앙) | 커버 시총(중앙) | 실패 달 | 지도 위반 달 |
|---|---|---|---|---|---|
| 2016 | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ |
| … | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ |
| 2026 | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ |

쓸 달 T = ⟨TBD⟩(⟨TBD: 첫 달⟩ ~ ⟨TBD: 끝 달⟩) · T < 84 면 R1 · R2 모두 측정 · 달 목록 sha ⟨TBD⟩ · 통제 패널 지문(y 없음) ⟨TBD⟩.
빠진 이름의 퇴출 사유(인수 · 시총 편출 · 비공개화 · 파산) ⟨TBD: 경로⟩.

**밀도(월 종목 수 · Stage M 표본 · k = 0)**

| 표지 | 규칙 | 중앙값 | 최소 | F0 |
|---|---|---|---|---|
| OS | 중앙값 ≥ 20 · 최소 ≥ 5 | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ |
| RS | 중앙값 ≥ 10(관문 i 의 조건) | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ |
| SALL | 보고만 | ⟨TBD⟩ | ⟨TBD⟩ | — |
| CH | 중앙값 ≥ 30 | ⟨TBD⟩ | ⟨TBD⟩ | ⟨TBD⟩ |
| FL | 보고만 | ⟨TBD⟩ | ⟨TBD⟩ | — |

보고만(R1): 분류 불가 비율 · 강제 매도 제외율 · 연도별 10b5-1 표지율(정규식 · 체크박스 · 불일치) · 공동 제출 수 · 분류일 뒤 도착 제외 수 ⟨TBD: 밀도 보고서 경로⟩.

**R2 외부 F0**: 연간 유효 짝 ≥ 900(경계 연도 2015~2025) ⟨TBD⟩ · 연도별 분리 실패 ≤ 20%(공개 연도 2016~2026) ⟨TBD⟩ · §C 파서 검증
(Item 1A 경계 단어수 ±10% 안 ≥ 90% · 형태 판정 ≥ 95% · LM 10-Q N_Words 순위상관 ≥ 0.95) ⟨TBD⟩ · 형태 분포(B/U/F) · 형태 전환 비율 연도별 ⟨TBD⟩.

**R2 경계 세계(카드 (7) «직전 달력연도에 공개된 세계 전체 짝»)**: `r_stagem.boundary_world`(2015-01..2026-07 · 비금융 · FPI 아님 · 지도가 푼 그룹 ·
가격 무관) 그룹-월 ⟨TBD⟩ · sha ⟨TBD⟩ · P0 개수 표(`r_p0.member_world`)와 첫 달 · 크기가 같아야 한다 ⟨TBD⟩ · 창 안 공개연도마다 경계가 그 세계 12달로 섰다
(`uncovered_all` 섞임 없음) ⟨TBD⟩.

**Stage S F0(잴 수 있는 편입의 교체 수 중앙값 2~15 · 41 편입)**: R1 ⟨TBD⟩ · R2 ⟨TBD⟩(3월 편입은 10-Q 공개가 적어 교체가 거의 없다 · 선언) · 셈 규칙 = `r_stages`
(모듈 F0 와 러너 F0 가 같아야 한다 · 모듈이 뺀 편입 ⟨TBD⟩) · 목표 해시(규칙 · 쌓은 팔 · R5 · V0 — 등록에 얼리고 굽기의 targets_hash 와 같아야 한다) ⟨TBD⟩ ·
Stage M 표지와 Stage S 묶음 표지(OS · CH)가 같은 칸에서 켜졌다 ⟨TBD: 칸 수 · 한쪽에만 0⟩ ·
그룹을 풀지 못해 세지 못한 상위 30 이름 수 ⟨TBD⟩ · FPI(걸러낼 수 없음) 편입별 수 ⟨TBD⟩.

**§G 일치 시험(2026Q2 · R1 전방 팔을 여는 조건)**: 행 일치 ⟨TBD⟩(≥ 99.5%) · 회사-월 OS 불일치 ⟨TBD⟩(≤ 1%).

**R5 사건 수(수익 없음 · 원천 하나 = Stage S 묶음의 ALARM · `r_r2flags.r5_events`)**: 이름-월 ⟨TBD⟩ · EG30 안 ⟨TBD⟩ · 업종 분포 ⟨TBD⟩ ·
4.01 본문 분류(사임 · 정례 · 본문 없음) ⟨TBD⟩.

**구조 점검**: 시총 핀(AMZN@2021-06 · TSLA@2021-11 · WMT@2019-06 · SHARES2) ⟨TBD⟩ · V0 명단 = Eg 순위 앞 30 어긋난 편입 ⟨TBD⟩/41.

## 9. 검정력 (미리 적는 현실)

- 10% 슬리브 · 120개월로 다중성을 넘으려면 새 효과만으로 펀드 IR 약 0.75~0.9 가 필요하다 — 공개 대형주 이례현상은 발표 뒤 · 대형주 할인 뒤 0.0~0.3.
  그래서 1차를 «세계 전체에서 신호가 수익을 예측하는가» 로 옮겼다(펀드 수준보다 검정력이 훨씬 크다). 펀드 단계는 전방으로만 판정한다.
- 사전 확률(카드 원문): R1 Stage M 통과 약 0.07 · R2 약 0.09 · 하나라도 약 0.15 · 전방 채택까지 둘을 합쳐 약 0.03~0.05.
- 통과해도 EG30 에 더해지는 몫은 연 +0.05~0.25%p 로 본다 — «급등락 양쪽에서 지수를 이긴다» 를 혼자 채울 크기가 아니다.
- **m = 0 도 미리 정한 정상 결과다.**

## 10. 전방 규칙 (표본 안은 채택하지 않는다 · 원장 PREREG-⟨TBD⟩-QFWD)

- R1 · R2 EG30 단계 팔은 **이 커밋에서 Stage M 을 굽기 전에** 규칙과 해시를 얼리고, Stage M 결과와 무관하게 원장에 넣는다
  (2026-10-30 20:00 UTC 전 · 늦으면 2026-12-31 편입부터). 교체 수를 맞춘 C3(모멘텀) · C4(고베타) 팔도 측정으로 기록한다(FF2 (g)용).
- 채택 경로는 §7 을 넘은 카드만 연다. 쌓은 팔 V0 + R1 + R2 는 측정이며, 두 기전이 모두 통과하고 각자 FF2 를 넘었을 때만 채택 경로를 가진다.
- R5 팔(V0 + 경보 제외)은 사건 기록용 측정 팔 · R3 는 원장에 넣지 않는다.
- FF1(24개월): 누적 X_규칙 ≤ 누적 X_V0 → 기각. FF2(36개월 이상 · 사건 최소 — 급락 다리 ≥ 3 · 반등 ≥ 3 · CRASH-M ≥ 4 · SURGE-M ≥ 4 · S&P 하락월 ≥ 10):
  (b) 전 월 Δ(대 V0 · β̂ 는 표본 안 측정값으로 고정) NW t ≥ 1 이고 하락월 Δ 점 ≥ 0 · (g) C3 · C4 를 모두 점으로 이긴다 · 나머지 (a)(c)(d)(e)(f)는 QFWD 와 같다.
  통과하면 새 등록 단계로 위성에 넣고 시계를 다시 시작한다(«전방 일치» · «확인» 아님).
- 전방 표지: R1 = `build/ins_daily.py`(§G · 등록 전 일치 시험 통과가 팔을 여는 조건) · R2 = 새 10-Q 가 나올 때마다 같은 파서(해시 고정).

## 11. 한계 · 위험 (미리 적는다)

- S&P 500 은 CMP «상위 절반» 보다 더 위쪽이고 CMP 의 가치가중 매도 결과는 약하다(5요인 −0.10 · t −0.49) · −0.55 의 기준은 매수만 있던 회사-월이다.
- Lazy Prices 표본(1995~2014)은 게재 전이다 · 주의 깊은 투자자가 많을수록 예측력이 약하다(CMN 표 VIII) · 거시 충격 분기(2020)에는 업종이 한꺼번에 빠질 수 있다.
- B/M 은 최신 제출본(재작성 누수) · 달 중간 상장폐지 = 마지막 가격 · 2016~2019 편출 가격 커버리지 · FPI 는 걸러낼 수 없다(편입마다 수 보고).
- 10b5-1 표기는 2023 개정 전 자율이었다 — 1차는 10b5-1 로 빼지 않는다(쌍둥이 T1 · T2 만 · 연도별 표류가 크다).
- 단위 시험이 코딩 오류의 유일한 방어다. 측정 팔 · 쌍둥이는 결과를 본 뒤 대안으로 읽힐 수 있다 — **더 좋아 보이는 팔은 결코 1차를 대신하지 않는다**.
- SEC 10-S 반기보고 안이 채택되면 10-Q 짝이 끊긴다(전방 R2).

## 12. 산출물 · 얼린 파일 · 러너 점검

- 한 커밋으로 계산 전에 커밋하고 푸시한다: 이 문서 · 카드 원문 · 러너 `build/r_run.py`(REG 채움) · 엔진 `build/r_stagem.py` · Stage S `build/r_stages.py` · P0 `build/r_p0.py` · `build/r_p0_adapt.py` ·
  표지 `build/r_r1_flags.py` · `build/r_r2flags.py` · 지도 `build/issuer_map.py` · 틀 · 세계(qbatch_core · eg30plus · qg_lab · pit_panel · pit_quarantine · stoploss ·
  tech_backtest · rally_pattern · index_members) · Stage S 측정 · 달력(qbatch_run · q_bmrot_leg · refresh_events) · 닫힘의 나머지(q_ltd · edgar · ml_core · ml_strats ·
  pit_backtest) · 자료 빌더 ⟨TBD: REG frozen_extra 목록⟩ · P3 · 새 자료(§2 표). 얼린 코드 목록은 러너가 굽기 경로의 **가져오기 닫힘**(함수 안 늦은 import 포함 ·
  정적)으로 다시 세어 대조한다 — 목록 밖 build/ 모듈이 닫힘에 있으면 `--dry` · 굽기가 멈춘다.
- **등록 순서**: ① REG["stage_s"] = "r_stages" 로 정한다(교체 수 · 목표 해시 셈 규칙 — F0 문서가 이 규칙으로 센다 · 실제 굽기는 "none" 을 받지 않는다)
  ② `r_run.py --f0` → data/_r_f0.json ③ `r_p0.py counts` · `run`(달 관문 = F0 의 쓸 달 · 외부 F0 = F0 문서 값 · 경계 세계 2015-01 부터) → data/_r_p0_counts.json ·
  data/_r_p0.json ④ REG 의 m · primary · p0 · snap · base · n_lab_prior · frozen_extra 를 채운다 ⑤ **`--check-reg`**(REG = P0 문서 = F0 문서 = 개수 표 경계 세계 ·
  양방향 · P0 재유도 · F0 자기 점검) · snap_wt 에서 **`--check-reg --rebuild`**(F0 를 다시 세어 바이트 대조) — 문제가 하나라도 있으면 ②~④ 로 돌아간다
  ⑥ 이 문서의 ⟨TBD⟩ 를 모두 채우고 이름의 날짜를 바꾼다(PREREG · PREREG_CARDS · RESULT 한 줄기) ⑦ `--selftest` · `--dry`(종료 코드 0 이어야 한다) · `--smoke-real`
  ⑧ 한 커밋 · 푸시.
- 러너(`RBATCH_COMMIT=<이 커밋>`)는 다음을 보고 하나라도 어긋나면 돌지 않는다 —
  ① 세 문서 이름이 자리표시자가 아니고 한 날짜 줄기인가 · REG 에 빈칸이 없는가 · REG stage_s 가 모듈인가 · 덮어쓰기 표지(data/_r_flags.json)가 없는가 ·
  가져오기 닫힘이 모두 얼린 목록 안인가 ② RBATCH_COMMIT 이 이 문서와 카드 원문을 처음 더한 커밋이고 origin/main 의 조상인가 ·
  이 커밋 판 문서에 ⟨TBD 가 없는가 ③ 산출물(data/_rbatch.json · RESULT)이 여기에도 origin 에도 없는가 ④ 시작 표식(재실행 · 이어하기는 같은 커밋의 표식이 있을 때만) ⑤ 얼린 파일 ·
  P3 가 이 커밋과 blob 수준으로 같은가(CRLF → LF 판 허용) ⑥ P1 · P2 핀 ⑦ 새 자료 · 선택 입력이 이 커밋과 같은가 · 고정표 링크를 모두 확인했는가 · (RBATCH_RAW 가 있으면) 원본 sha256
  ⑧ numpy 2.5.3 · scipy 1.18.1 ⑨ PYTHONHASHSEED=0 · OPENBLAS_NUM_THREADS=1.
- 시작 표식 전에(수익 없음): REG = P0 문서 = F0 문서(m · 가족 · P0 재유도 · 카드마다 T · 밀도 · 외부 F0 · result_sha · 경계 세계) · F0 문서 자기 점검.
  긴 계산 전에: 시총 핀 · 쓸 달 · 통제 패널 지문 · 표지 지문 · F0 재현(바이트 · 경계 세계 · Stage S 목표 해시 · 표지 일치 포함) · 등록된 측정 입력
  (R1 쌍둥이 · 표본 칸 · R2 대조 셋 · R3 §D 판)이 모두 있는가. Stage M 뒤: 선언하지 않은 못 돌린 시도가 없는가. Stage S 뒤: 목표 해시 = 등록 F0.
- **검문점**: 카드마다 결과를 `%TEMP%/rbatch_ckpt/<커밋>` 에 머리(커밋 · 카드 · 가져오기 닫힘 코드 sha · 패널 지문 · 달 sha · 표지 · 대조 지문 · RS 밀도 F0 · 해시 씨앗 · BLAS)와 함께 둔다 —
  **산출 전 기술적 중단**에서만 `RBATCH_RESUME=사유`(이어하기) 또는 `RBATCH_RERUN=사유`(처음부터). 머리가 다르면 멈춘다. 새로 시작하면 검문점을 먼저 지운다.
- → `data/_rbatch.json`(판정 · 주 통계 · Holm · 관문 · 측정 · γ 월 계열 · F0 · 핀 · 시도 수) · 결과 `build/PREREG-⟨TBD⟩-RBATCH-RESULT.md`.

## 13. 구현 해석 · 선언 (등록 전에 정했다 · 성과는 아무도 보지 않았다)

**러너(`build/r_run.py`)**
- 한쪽 p 의 자유도는 T − 1(T = γ 가 선 달 수)이다 — T = 120 이면 카드 문구의 t(119) 와 같다. P0 식의 2.27 은 상수로 둔다(T 가 줄어도 바꾸지 않는다 · r_p0 와 같다).
- R1 관문 i 는 RS 밀도 F0(중앙값 ≥ 10)가 참일 때만 판정한다 — 아니면 거짓(닫힌 실패).
- R2 연간 유효 짝 ≥ 900 은 경계 분포가 쓰이는 해(2015~2025)에, 분리 실패 ≤ 20% 는 표지가 쓰이는 공개 연도(2016~2026)에 건다.
- NW(3) t 는 달력 위치를 지킨다 — 이어진 달은 `eg30plus.nw_t`, 빈 달이 끼면 `r_stagem.nw_t_gap`(§4). 러너 `primary_stat` 은 엔진 fm 의 달 목록을
  받아 같은 정의로 다시 계산하고 엔진 요약과 1e-9 로 대조한다(압축 계열 값은 보고만). 모든 측정 판의 요약도 같은 정의다.
- R2 경계 분포의 세계는 `r_stagem.boundary_world(Wd, IM, 2015-01, 2026-07)` 한 벌이다 — 1차 CH · 대조 셋(`r_stagem.r2_variants` → `stage_m_r2`
  fp_doc · fp_ixbrl · fp_covid) · 외부 F0(`r_r2flags.r2_density`) · Stage S 묶음(`r_stages.load_bundle(r2_world=…)`)이 모두 이것을 쓴다. 세계가 2015-01 부터
  해마다 그룹-월을 담지 않거나, 창 안 공개연도의 정본 경계가 그 세계 12달로 서지 않으면(`uncovered_all` 섞임) 러너가 멈춘다. 세계의 지문 · 크기 ·
  연도별 수를 F0 문서에 얼리고, P0 개수 표(`r_p0.member_world`)의 첫 달 · 크기와 같아야 굽는다(σ_placebo 를 잰 CH 와 굽기의 CH 가 한 경계).
- R1 쌍둥이 T1~T6 은 cikmonth.json 한 판의 칸이고 엔진 `stage_m_r1` 이 한 번씩 돌린다(T2 의 2023-04 전후 분할은 한 시도 안의 보고). 러너는 따로
  쌍둥이 회귀를 돌리지 않는다. R1 관문 i 의 RS 밀도 F0 는 엔진에도 넘긴다(`rs_f0_ok`).
- 시도 수 = 엔진(`stage_m_r1` · `stage_m_r2`) · 러너(R3) 시도 목록의 돈 줄 + Stage S 팔(카드 controls: R1 규칙 · C1~C4 · 위약 · R2 규칙 · C2~C5 · 위약 ·
  R12 쌓은 팔). 못 돌린 시도는 사유와 함께 싣는다. 선언한 못 돌린 시도: R1 · R2 2014-07 넓힌 창(편출 가격 복구 전 · §4) · **R3 Stage S 세 팔(매월 빼기 ·
  위약 1000 · 고베타 빼기 — `r_stages` 에 없다 · 결과 문서에 «등록 오류(미구현 측정)» 로 적는다 ⟨TBD: 등록 전에 r_stages 에 더했는가⟩)**. 그 밖의
  못 돌린 시도가 있거나 등록된 측정 입력이 없으면 굽지 않는다(등록 오류).
- Stage S 는 `r_stages` 모듈로만 굽는다(REG stage_s = "none" 은 합성 · 연기 시험 전용 — 실제 굽기는 거부). 교체 수(F0)는 `r_stages.build`(수익 없음)의
  cards[코드].f0.n_r 가운데 **모듈이 뺀 편입(excluded — 창 정의 · 지도 F0 · §F 밖) 밖** 만 세고, 모듈 F0 판정과 러너 판정이 같아야 한다. 목표 해시
  (cards[*].hash · v0_hash)를 F0 문서에 얼리고 굽기의 targets_hash 와 같아야 한다(forward_plan «규칙과 해시를 등록 커밋에서 얼린다»). Stage M 표지와 Stage S
  묶음 표지가 같은 (그룹, 달)에서 켜졌는지(OS · CH) 대조한다 — 다르면 멈춘다. 측정 팔은 `r_stages.run` 을 등록 뒤 한 번(검문점 "S") 부르고
  20bp(fr20 · V0_20) · PR(fr_pr) 행을 함께 싣는다(카드 «20bp 행을 함께 기록»).
- R5 이름-월 · EG30 안 수 · 업종 분포는 Stage S 묶음의 ALARM(정본 `r_r2flags.r5_events`) 하나에서 센다(Stage S R5 팔과 같은 원천).
- 채택 경로는 Stage S F0 가 참일 때만 연다(«벗어나면 측정 불가» 를 채택 경로에도 건다).
- 러너는 엔진(r_stagem)의 요약 · 관문을 다시 계산해 대조한다 — 다르면 멈춘다. P0 문서는 strict 로 읽는다(result_sha 칸 필수 · 상수 = 카드 식 ·
  카드마다 식 재유도 · 개수 출처가 정본(canonical) · 덮어쓰기 · 간이판이면 멈춤).
- 등록 전 대조는 가족 안팎 모두(양방향): P0 카드 T · 밀도(R1 = OS · RS · R2 = CH) · R2 외부 F0 세 칸 · eligible 이 F0 문서와 맞아야 한다.

**표지 빌더(선언 전문은 각 파일 머리말 — 여기에 옮긴다)**
- R1(`r_r1_flags.py` a~j): ⟨TBD: 분류 적용 연도 · 공동 제출 배정 · 정정 중복 · 관계 부분 문자열 · A/D · N(자료 부족) · 10b5-1 표지 · 금액 · FPI null · Stage S 채움⟩.
- R2(`r_r2flags.py` ①~⑧ · R5 ⑨~⑫): ⟨TBD: 같은 기간 중복 · 늦은 짝 · 조기폐장 마감 · CIK 효력 구간 · SimDoc · Δlog · 경계 세계 · 접수 시각 없음⟩.
- 엔진(`r_stagem.py`): ⟨TBD: 결측 정책(drop · dummy) · 모름 더미 · 종속 열 순서 · MIN_DOF · 52주 고점 정의(배당조정 종가) · 커버리지 시총 12개월 잇기⟩.
- P0(`r_p0.py`): ⟨TBD: 위약의 달 독립 뽑기 · n1 · 3분위 칸 · 정규 근사⟩.

## 14. 등록 전 적대 검토 (계산 전 · 성과를 본 사람 없음)

**러너 1회차(2026-09-25 · 합성 자료 · 구성만)** — 높음 2 · 중간 4 · 낮음 3. 모두 고쳤다(§13 에 반영).
- 높음: 러너가 압축 계열 NW t 를 쓰고 엔진은 빈 달에 nw_t_gap 을 써서, 빈 달 하나(흩어진 커버리지 실패 · 자유도 건너뜀 · R3 표지 0 달)로 굽기가
  Stage M 뒤에 멈췄다 → 한 정의(달력 위치)로 맞추고 세 갈래 빈 달을 합성 시험에 넣었다.
- 높음: R2 경계 세계를 패널 행(2016-08 부터 · 가격 있는 행)으로 넘겨 2016 경계가 §C 전체 · 2017 경계가 반쪽 세계로 섰다 → boundary_world 한 벌 ·
  경계 출처 점검 · F0 에 세계 지문 · P0 개수 표와 대조.
- 중간: R1 쌍둥이를 따로 파일로 두어 T1 · T2 · T6 을 두 번 셌다(17 대 12) · R2 대조를 엔진 밖에서 돌렸다 → 엔진 한 벌 · 시도 목록으로 센다.
- 중간: 굽기 경로 모듈(qbatch_run · refresh_events 등)과 선택 입력(_px_raw · _t401 · _r_coverage)이 핀 밖이었다 → 가져오기 닫힘 핀 · 선택 핀 · 덮어쓰기 금지.
- 중간: 등록 전 REG · P0 · F0 대조가 없고 한쪽 방향이었다 → `--check-reg`(양방향 · --rebuild) · `--dry` 종료 코드.
- 중간: 연기 시험이 Stage S 를 돌리지 않았고 20bp 행이 빠졌다 → 합성 표지 blind_smoke · fr20 · fr_pr 싣기.
- 낮음: P0 result_sha 가 칸이 없으면 조용히 건너뛰고 경계(0.1499996)에서 헛멈춤 · 식 재유도 없음 → strict · p0_pass · 재유도. 이름 점검이 PREREG 만 봤다 →
  세 이름 · 한 줄기. Stage S «none» 이 측정을 조용히 버렸고 R5 원천이 둘 · 목표 해시가 F0 에 없었다 → 못 돌린 시도 싣기 · 원천 하나 · 해시 얼림.

⟨TBD: 다음 회차 — 러너 정확성 · 모듈 연결 · 규율 — 치명 · 주요 · 사소 수와 바꾼 것⟩
