# 사전등록 — 거시 요인 둔감 2종 · 2026-08-12, 돌리기 전에 확정

규칙 **87종** → 89종, 임계 3.44 → **3.45**. PIT 족 62 → 64.

## 0. 오염 상태 · **이번에는 세 목록을 다 훑었다**

두 규칙의 수익률을 한 번도 계산하지 않았다.

🚨 5차까지 다섯 배치가 전부 **라이브 목록만** 훑고 "0건이니 빈 칸"이라 적었다. 그 결과
`x-reta`(선반에 있던 규칙)를 재등록했고, `x-amihud`(선반의 `x-illiq` 을 개명한 것)는
소급 t 6.84 로 **게시 직전까지** 갔다(DATA-FACTS #27).

→ 그래서 `build/tried.py` 를 만들었다. 세 목록을 한 번에 훑는다:

    살아있음 87 · 선반 15 · 퇴출 13 = **115종**

이 배치는 그것을 돌리고 시작한다. 실측:

    금리 1건(x-lowde 의 산문 언급뿐) · 달러 0 · 인플레 0 · 거시 0 · 매크로 0 · DGS 0 · 듀레이션 0

⚠ 오늘 1~5차 결과를 봤다. 영향은 4·5차와 같다 — 이웃 지도와 세 목록을 보고 **빈 칸**을 고른다.

## 1. 왜 이것인가 — 이 랩의 베타는 전부 **시장** 베타다

115종의 베타·상관 규칙 전부:

| 규칙 | 무엇에 대한 베타인가 |
|---|---|
| `x-lowbeta` 저베타 틸트 | 시장 |
| `x-lowcorr` 시장 저상관 | 시장(상관 성분만) |
| `x-updown` 상하방 베타 비대칭 | 시장(방향별) |
| `x-peerlag`(퇴출) 상관이웃 선도-지연 | 시장·동료 |

**시장 밖 요인에 대한 노출을 재는 규칙이 하나도 없다.** 그런데 자료는 있다 —
`data/assets.json` 의 `macro` 에 DGS10·DGS2·DTWEXBGS 가 **1990-01 부터 일간 9,157점**이다.

## 2. 규칙 — 둘. 변형 없음.

> Frazzini·Pedersen (2014), "Betting against beta", *JFE* 111(1).
> Baker·Bradley·Wurgler (2011), "Benchmarks as Limits to Arbitrage", *FAJ* 67(1).
> Chen·Roll·Ross (1986), "Economic Forces and the Stock Market", *Journal of Business* 59(3).

### 공통 — 베타 추정

    창 252거래일 · 일간 수익률을 거시 계열의 **일간 변화**에 단순회귀한 기울기
    두 계열이 같은 날 모두 있는 관측만 쓴다 · 유효일이 창의 60% 미만이면 후보 제외

⚠ **금리는 차분(%p), 달러지수는 비율변화**를 쓴다. 금리는 %단위 수준이라 비율변화를 쓰면
  저금리 구간에서 폭발한다. 이 구분을 결과를 보고 바꾸지 않는다.
⚠ 결측일은 직전 값을 끌고 가지 않는다 — 끌고 가면 '움직이지 않았다'는 가짜 관측이 생겨
  베타를 0 쪽으로 당긴다.
실측 유효일: DGS10 4,362/4,428 · DTWEXBGS 4,304/4,428.

### ① `x-ratebeta` — |10년물 베타| 최하위 10   ### ② `x-fxbeta` — |광의 달러지수 베타| 최하위 10

    신호 = −|베타|          (절댓값이 작을수록 위로)
    최하위 10종목 동일가중 · 월말 리밸런스

논거: 저베타·저변동성 이례현상의 논리를 **시장 밖 요인**으로 넓힌다 — 위험 노출이 낮은
자산이 위험조정 기준으로 더 나았다는 것이 그 계열의 주장이다.

🚨 **방향은 절댓값 최하위다.** 부호 있는 최하위(금리 상승에 가장 취약한 쪽)를 나중에
   시도하지 않는다 — 그건 다른 규칙이고, 결과를 보고 부호를 고르면 그 검정은 무효다.
🚨 둘을 같이 등록하는 이유: 같은 결과를 내면 '거시 둔감'이라는 축이 하나인 것이고,
   갈리면 요인마다 다른 것을 재고 있다는 뜻이다(`x-sue`·`x-epsacc` 를 같이 실은 것과 같은 이유).

## 3. 밀도 · PIT

| 규칙 | 후보 중앙 | 문턱 미만 | 처음 도는 달 | **PIT** |
|---|---|---|---|---|
| `x-ratebeta` | 480 | 12/211달 | 2010-01 | ✅ 가능 |
| `x-fxbeta` | 480 | 12/211달 | 2010-01 | ✅ 가능 |

**PIT 가능한 이유**: 거시 계열은 전 종목이 공유하는 **하나의 계열**이다 — 종목별 자료가
아니므로 편출 종목에도 그대로 쓸 수 있다. 종가만 더 있으면 되고 그것은 캐시에 있다.
(`x-amihud` 가 막힌 벽은 거래량이 **종목별**이라 편출분이 없다는 것이었다. 성질이 다르다.)

## 4. 게시 기준 · 미리 정하는 것

    ⓐ |t| > **3.45**(89종)   ⓑ incr5.t ≥ 2.0   ⓒ PIT 레그도 통과

- 하나라도 못 넘으면 기각으로 싣는다. 창(252일)·상위 N(10)·부호·요인을 바꾼 변형을 돌리지 않는다.
- `pit_backtest.PRICE_SIDS` 에 **등록과 함께 미리** 넣는다.
- `arch` 를 붙인다(`macro-factor-beta`) — 다음 사람이 이름을 바꿔 재등록하면 검사가 잡도록.

## 5. 미리 적는 예측

- 둘의 이웃에 **`x-lowbeta`·`x-lowvol`·`x-ivol`** 이 잡힐 것으로 본다. 거시에 둔감한 종목은
  대체로 변동성 자체가 낮기 때문이다. 그러면 incr5 에서 죽는다.
- `x-ratebeta` 와 `x-fxbeta` 는 **서로의 이웃**이 될 것으로 본다.
- ⚠ 예측이 맞든 틀리든 판정 기준은 안 바꾼다.
