# 결과 — 스타일 8종의 가중 방식 · 2026-09-18 · **셋 다 기각(F2·F3·F4)**

풀카드: 없음
판정: 기각
규칙: x-style8w-iv, x-style8w-erc, x-style8w-mv

등록: `build/PREREG-2026-09-18-STYLE8W.md`
구현: `build/style8w.py` → `data/_style8w.json`(얼린 측정 · 커밋 안 함)
천장 진단: `build/style8_oracle.py` → `data/_style8_oracle.json`

계산 전 | 커밋 `589dffca9fb3f3a036f3c49862fd66fdc71c233a` (2026-09-19) ·
등록 문서 블롭 `800607ebafb4b8233ba189a8fd86ba22663864a5`

🚨 **이 해시는 「2026-09-18 에 계산 전이었다」를 증명하지 않는다 — 읽는 법을 적는다.**

원래 이 자리에는 이렇게 적혀 있었다.

> 🚨 **계산 전 커밋이 없다.** 등록 문서를 쓰고 **커밋하지 않은 채** 돌렸다. … 등록을
> 먼저 커밋하고 **다시 돌려야** 완전해진다 — 규칙·실패조건·예측은 돌리기 전에
> 확정했고 그 뒤 고치지 않았지만, 그것을 증명할 수단이 없다.

**2026-09-19 에 그 말대로 했다.** 등록 문안을 **한 글자도 고치지 않고** 커밋하고
(`589dffc`), 그 뒤 `build/style8w.py` 를 다시 돌렸다.

**재실행 결과는 `data/_style8w.json` 과 바이트 단위로 동일하다**(3,780자 · 전수 대조).
판정도 그대로다 — iv·erc·mv 셋 다 기각(F2·F3·F4).

그러므로 이 해시가 증명하는 것은 딱 이것이다.

| 증명되는 것 | 증명 안 되는 것 |
|---|---|
| 2026-09-19 재실행 **전에** 이 문안이 고정됐다 | 2026-09-18 **첫 실행 전에** 고정됐다 |
| 그 문안으로 돌린 결과가 지금 실린 수치다 | 첫 실행과 지금 사이에 문안이 안 바뀌었다 |

**「그때 고치지 않았다」는 여전히 내 말일 뿐이다.** 다만 계산이 결정적이고 재실행이
바이트 단위로 재현됐으므로, **수치가 문안에 맞춰졌을 가능성은 배제된다**(맞췄다면
고정된 문안으로 다시 돌렸을 때 같은 값이 나올 이유가 없다). 남는 것은 「실패조건을
결과에 맞춰 느슨하게 썼을 가능성」이고, 그것은 이 해시로 못 막는다.

⚠ **늦게 커밋한 것을 제때 커밋한 것처럼 읽지 말 것.** 이 문서는 랩에서 유일하게
사후 커밋으로 메운 판이다. 다음부터는 돌리기 전에 커밋한다.

---

## 0. 한 줄

**가중 방식 셋이 전부 기각됐고, 그보다 나쁜 소식이 있다 — 벤치가 넷을 다 이겼다.**
표본 밖 49개월에서 샤프가 벤치 0.92 · `ew` 0.89 · `erc` 0.89 · `iv` 0.87 · `mv` 0.65 다.

그리고 **왜 그런지는 성적을 보기 전에 §1-0 이 이미 말하고 있었다** —
8종의 **유효 자산 수가 2.45 / 8** 이다. 가중을 바꿀 것이 거의 없었다.

## 0-1. 🚨 F7 — 소급 레그로 쟀다면 다른 답이 나왔다

사용자 지시(2026-09-18)로 백테스트는 **PIT 레그로만** 돌렸다. 천장 진단이 두 레그를 다 재서
남긴 것:

| | PIT | 소급 |
|---|---|---|
| 스타일 순위 | 성장 > 모멘텀 > 멀티팩터 > 고배당 > 고베타 > 중소형 > 퀄리티 > 가치 | **중소형** > 모멘텀 > 고베타 > 성장 > 멀티팩터 > 고배당 > 퀄리티 > 가치 |
| 8종 동일가중 총수익 | 111.3% | **382.9%** (3.4배) |
| 8종 동일가중 샤프 | 0.65 | 1.22 |

중소형이 소급 1위 · PIT 6위다. **소급 계열 위에 가중이든 로테이션이든 얹으면 중소형을
가장 좋은 자산으로 배운다.** 그 순위는 PIT 에서 성립하지 않는다.

---

## 1. 판정

측정 구간 — 표본 밖 **2022-09-01 ~ 2026-09-16 · 49개월 · 재조정 49회.**
(패널 2021-07-30~2026-09-16 에서 공분산 warm 252거래일을 뺀 것)

| | 조건 | `iv` | `erc` | `mv` |
|---|---|---:|---:|---:|
| **F1** | `ew` 를 샤프에서 넘을 것 | −0.015 기각 | **+0.004 통과** | −0.238 기각 |
| **F2** | `ew` 대비 초과의 t ≥ 1.5 | t −0.95 기각 | t −0.72 기각 | t −1.40 기각 |
| **F3 🚨** | 무작위 라벨 셔플 200회 상위 5% | **41.5%** 기각 | **54.5%** 기각 | **22.0%** 기각 |
| **F4** | 왕복 20bp 뒤 유지 | −0.015 기각 | +0.003 기각(F2 불통과) | −0.250 기각 |
| F5 | 집중도 | 최대비중 중앙 16.3% | 17.6% | 🚨 **46.7%** |
| F6 | 월 회전율 | 2.2% | 2.3% | **10.2%** |
| F8 | 유효 자산 수 (셋 공통) | 2.45 / 8 | 2.45 / 8 | 2.45 / 8 |

🚨 **`erc` 의 F1 통과는 +0.004 다.** 샤프 셋째 자리에서 갈린 수이고, 같은 칸의 월 초과수익은
**−0.092%p 로 음수**다. 즉 수익이 나아진 것이 아니라 변동성이 18.2% 로 줄어 샤프가 소수점
아래에서 뒤집힌 것이다. **「덜 흔들렸다」를 「나았다」로 읽지 않는다.**

## 2. 성적 (표본 밖 49개월 · 비용 전)

| | 총수익 | 연율 | 변동성 | **샤프** | 월 회전 | 최대비중 중앙 | 비용 p.a. |
|---|---:|---:|---:|---:|---:|---:|---:|
| **벤치**(PIT 시총가중) | 90.4% | 17.1% | **14.3%** | **0.92** | — | — | — |
| `ew` 1/8 | **117.4%** | **20.9%** | 19.5% | 0.89 | 2.0% | 12.5% | 0.05%p |
| `erc` | 109.9% | 19.9% | 18.2% | 0.89 | 2.3% | 17.6% | 0.06%p |
| `iv` | 107.7% | 19.6% | 18.3% | 0.87 | 2.2% | 16.3% | 0.05%p |
| `mv` | 72.2% | 14.2% | **17.1%** | 0.65 | 10.2% | 46.7% | 0.25%p |

⚠ **총수익은 `ew` 가 벤치를 27%p 이겼는데 샤프는 졌다.** 벤치 변동성이 14.3% 로 훨씬 낮다.
8종이 각각 상위 10종목 집중이라 그 위에 어떤 가중을 얹어도 벤치의 분산을 못 따라간다.

### 2-1. 평균 비중 (49회)

| | `ew` | `iv` | `erc` | `mv` |
|---|---:|---:|---:|---:|
| 고배당 | 12.5% | 15.7% | 16.7% | **37.5%** |
| 멀티팩터 | 12.5% | 15.5% | 14.7% | 23.7% |
| 모멘텀 | 12.5% | 11.7% | 12.8% | 15.8% |
| 가치 | 12.5% | 14.3% | 14.3% | 15.1% |
| 퀄리티 | 12.5% | 12.4% | 11.9% | 6.9% |
| 성장 | 12.5% | 12.2% | 11.4% | 0.9% |
| 중소형 | 12.5% | 11.2% | 10.9% | 0.1% |
| 고베타 | 12.5% | 7.1% | 7.2% | **0.0%** |

`mv` 는 고배당·멀티팩터 둘에 61.2% 를 싣고 고베타·중소형·성장을 사실상 0 으로 만들었다.
**F5 가 발동한다 — 사실상 두 스타일이다.** 그리고 그 집중이 성적을 만들지 못했다(샤프 0.65).

---

## 3. 🚨 F3 — 이 등록의 본론

비중 벡터는 그대로 두고 **자산 라벨만** 월별로 무작위 치환했다(200회). 비중의 모양(집중도·
회전율)은 보존되고 「이 비중이 이 자산 것」이라는 정보만 파괴된다.

| | 셔플 중앙 | 셔플 95% | 셔플 최고 | **실측** | **백분위** |
|---|---:|---:|---:|---:|---:|
| `iv` | 0.88 | 0.96 | 0.98 | 0.87 | **41.5%** |
| `erc` | 0.88 | 0.95 | 0.99 | 0.89 | **54.5%** |
| `mv` | 0.80 | 1.13 | 1.27 | 0.65 | **22.0%** |

**세 판 다 무작위의 한가운데이거나 그 아래다.**
🚨 그리고 **무작위 라벨로도 샤프가 최고 0.99~1.27 까지 나온다.** 셔플 없이 `mv` 의 어느
구간 성적을 봤다면 성공으로 읽었을 것이다.

⚠ **이 랩이 같은 방식으로 기각한 것이 세 번째다.**

| | 백분위 |
|---|---:|
| `REGIME0`(2026-09-07) A7 국면 | 50.9% |
| `FACROT`(2026-09-09) 상태 조건부 팩터 | **54.0%** |
| **`STYLE8W`(오늘) `erc`** | **54.5%** |

세 번 다 무작위의 한가운데다. 축이 국면 → 팩터 상태 → **자산 가중**으로 바뀌었는데
같은 자리에서 같은 수가 나왔다.

## 4. 왜 그런지 — §1-0 이 성적 전에 답을 갖고 있었다

등록 §0 이 「가중은 자산들이 실제로 다를 때만 일한다」고 적고 겹침을 먼저 재라고 못박았다.

| | 실측 |
|---|---|
| 월간 수익 상관 | 최소 0.23 · 중앙 **0.55** · 최대 0.82 |
| **유효 자산 수**(상관 고유값) | **2.45 / 8** |
| 평균 자카드(명단 겹침) | **0.029** (2026-08-31) · 0.030 (2026-09-16) |
| 8종 합집합 종목 수 | 69종 / 80칸 |

🚨 **명단은 거의 안 겹치는데(자카드 0.03) 수익은 크게 겹친다(상관 중앙 0.55).**
8종이 서로 다른 종목을 담는데 같은 것을 벌고 있다. 지수 방법론이 다른 자를 쓸 뿐,
그 자들이 결국 같은 소수의 공통 요인을 재고 있다는 뜻이다.

**유효 자산 수가 2.45 라면 8자산 가중 문제가 아니라 2~3자산 가중 문제다.**
거기서 `ew`·`iv`·`erc` 가 갈릴 자리가 거의 없고, 실제로 셋의 샤프가 0.87~0.89 로 붙었다.

⚠ 전 구간 명단 겹침은 못 쟀다. `ST.backtest` 가 월별 선정 명단을 돌려주지 않는다 —
다시 짜면 백테스트가 두 벌이 되므로 짜지 않았다. 필요하면 `ST.backtest` 가 `picks` 를
같이 돌려주게 하는 것이 맞고, 그것은 별도 작업이다.

---

## 5. 예측 판정

| | 예측 | 실측 | |
|---|---|---|---|
| **P1** | `erc` 가 F1 을 넘는다 | +0.004 | **맞음 — 그러나 사실상 0** |
| **P2** | `mv` 가 고배당·퀄리티에 쏠린다 | 고배당 37.5% · **퀄리티 6.9%** | 절반 맞음 — 둘째는 멀티팩터(23.7%) |
| P3 | F3 은 방향 예측 안 함 | 41.5 / 54.5 / 22.0% | — |
| P4 | 소급과 PIT 판정이 갈린다 | 순위 전면 재배열 · 3.4배 | **맞음** |
| **P5** | 유효 자산 수 4~6 | **2.45** | **빗나감 — 훨씬 낮다** |

🚨 **P5 가 빗나간 것이 이 등록의 핵심 발견이다.** 「8종이 뚜렷이 다른 것을 담고 있다」는
전제가 틀렸다. 그 전제가 틀렸으므로 가중 방식의 상한 자체가 낮았고, 성적은 그 결과다.

## 6. 판정

**`iv`·`erc`·`mv` 셋 다 기각(F2·F3·F4).** 게시하지 않는다.
**`ew`(1/8 동일가중)를 손대지 않는다** — 셋 중 어느 것도 그것을 유의하게 넘지 못했다.

**값은 성적이 아니라 넷이다.**

1. **유효 자산 수 2.45.** 이 랩이 스타일 8종의 «다름»을 처음으로 수치로 쟀다.
   명단은 안 겹치는데 수익이 겹친다는 사실이 그 자체로 산출물이다.
2. **셔플 백분위 54.5% — 세 번째 한가운데.** 축을 바꿔도 같은 수가 나온다.
3. **벤치가 넷을 다 이겼다**(샤프 0.92). 스타일 8종을 어떻게 섞어도 그 달 시총가중 지수의
   위험대비 성적을 못 넘었다. 총수익은 이겼고 변동성에서 졌다.
4. 🚨 **소급 레그였다면 다른 답이 나왔을 것이다**(§0-1). 사용자 지시로 PIT 만 쓴 것이
   결론을 바꿨다.

## 7. 다음 등록의 조건 — 🚨 로테이션은 만들지 않는다

등록 §7 이 「`STYLE8ROT` 은 이 결과 없이 시작하지 않는다」고 적었다. 그 결과가 나왔고,
천장 진단이 같이 답했다:

| | 값 |
|---|---:|
| 오라클(매월 1등을 미리 앎) 샤프 | 3.63 |
| 오라클 대 `ew` 월 초과 · t | +7.28%p · **12.83** |
| 월 최고−최저 스프레드 중앙값 | 11.95%p |
| **t ≥ 1.50 에 필요한 월 랭크 IC** | **0.138** |
| **t ≥ 3.30 에 필요한 월 랭크 IC** | **0.368** |

**천장은 문제가 아니다. 효과크기가 문제다.** `LAB-USAGE-MAP` §5 가 적은 문헌 최고 팩터
타이밍 IR 0.4~0.5 가 그대로 성립해도 이 창(약 4.1년 표본 밖)에서는
t = 0.5 × √4.1 ≈ **1.01** 로 F2(1.5)조차 못 넘는다.

🚨 **그리고 유효 자산 수 2.45 는 로테이션에 더 나쁜 소식이다.** 고를 것이 8개가 아니라
실질 2~3개라면, 「1등을 고른다」의 정보량이 그만큼 작다.

**그러므로 로테이션 등록을 만들지 않는다.** 이 문서와 천장 진단이 그 자리를 대신한다 —
`LAB-USAGE-MAP` §6 「쓸 데가 없는 것 — 그것도 결과다」에 넣는다.

## 8. 한계

- **표본 밖 49개월뿐이다.** 랩 표준 120개월의 절반 아래다. F2 의 t 가 그 얇음을 그대로 받는다.
- 🚨 **창을 바꾸면 벤치 비교가 뒤집힌다.** 전 구간 63개월에서는 `ew` 0.65 > 벤치 0.50 인데,
  공분산 warm 을 뺀 49개월에서는 벤치 0.92 > `ew` 0.89 다. **떨어져 나간 14개월이
  스타일이 이긴 구간이다.** 결론은 창에 민감하고, 그 사실을 성적과 같은 줄에 둔다.
- **셔플 200회**라 백분위 해상도가 0.5%p 다.
- **비용을 스타일 간 비중 변경분에만 걸었다.** 레그 내부 회전(상위 10 교체)은 레그 안에
  이미 들어 있고 네 판이 공통이라 차이에 영향이 없지만, 절대 수준은 낙관적이다.
- **`mv` 의 Ledoit-Wolf 축소강도 중앙값이 0.024** 로 작다 — 일별 252관측에 자산 8개라
  표본 공분산이 이미 잘 서서 축소가 거의 안 걸렸다. 즉 `mv` 는 사실상 표본 최소분산이다.
- **편출 종목 가격 보유율 최저 90.9% · 중앙 95.8%**(멤버 대비). 미확보 55종은 주입 못 했다.
- **무위험은 `data/rf_monthly.json` 의 전 구간 평균 하나**(월 0.310%)다. 시변이 아니다 —
  랩 전체가 쓰는 규약을 그대로 따랐다.

## 9. 안 하는 것

- 🚨 **공분산 창을 126·504 로 바꿔 다시 재지 않는다**(등록 §6). 252 하나였다.
- 🚨 **개별 상한을 넣어 `mv` 를 살리지 않는다.** 46.7% 쏠림은 결과다.
- 🚨 **셔플에서 졌다고 다른 셔플 방식으로 다시 재지 않는다.**
- 🚨 **`erc` 의 F1 +0.004 를 근거로 게시하지 않는다.**
- 🚨 **소급 레그로 다시 돌려 좋은 쪽을 고르지 않는다** — 그것이 `nsel` 이다.
- **비중 방식을 다섯째로 늘리지 않는다.**
- **8종을 바꾸거나 숨김 8종을 넣어 다시 재지 않는다.**
- **로테이션(조건부) 등록을 만들지 않는다**(§7).

---

**재현**: `python build/style8_oracle.py --check-anchor` → `python build/style8w.py`
⚠ 이 PC 의 Anaconda 는 `Library\bin` 을 PATH 에 넣어야 numpy 가 뜬다.
